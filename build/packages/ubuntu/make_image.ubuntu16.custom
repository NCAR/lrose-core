#! /bin/csh

#
# build docker image for ubuntu, with required packages
#

cd ~/git/lrose-core/build/packages/ubuntu

docker build \
    --tag ubuntu16:custom \
    --file Dockerfile.ubuntu16.custom .


