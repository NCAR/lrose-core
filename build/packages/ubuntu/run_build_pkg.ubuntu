#! /bin/bash

# call script to actually build package

os_version=$1
lrose_package=$2

# update git

cd ~/git/lrose-core/
git pull

# go to ubuntu dir

cd build/packages/ubuntu

# run the build script

./build_pkg.ubuntu $os_version $lrose_package
