#! /bin/csh

#
# build core in centos7 image
#

cd ~/git/lrose-core/build/packages/centos

docker image rm centos7:build

docker build \
    --tag centos7:build \
    --file Dockerfile.centos7.build.core .

