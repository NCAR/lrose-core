#! /bin/csh

#
# build docker image for centos, with required packages
#

cd ~/git/lrose-core/build/packages/centos

docker build \
    --tag centos7:custom \
    --file Dockerfile.centos7.custom .


