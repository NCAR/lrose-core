#! /bin/bash

# call script to build package

os_version=$1
lrose_package=$2

# update git

cd ~/git/lrose-core/
git pull

# go to debian dir

cd build/packages/debian

# run the build script

./build_debian_pkg $os_version $lrose_package
