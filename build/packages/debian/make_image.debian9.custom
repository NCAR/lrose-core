#! /bin/csh

#
# build docker image for ubuntu, with required packages
#

cd ~/git/lrose-core/build/packages/debian

docker build \
    --tag debian9:custom \
    --file Dockerfile.debian9.custom .


