#! /bin/bash

#
# build lrose in debian image
#

os_version=$1
lrose_package=$2

cd ~/git/lrose-core/build/packages/debian

docker image rm ${os_version}:build

docker build \
    --tag ${os_version}:build \
    --file Dockerfile.${os_version}.build.${lrose_package} .

