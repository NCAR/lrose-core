#! /bin/csh

#
# build blaze in debian9 image
#

cd ~/git/lrose-core/build/packages/debian

docker image rm debian9:build

docker build \
    --tag debian9:build \
    --file Dockerfile.debian9.build.blaze .

