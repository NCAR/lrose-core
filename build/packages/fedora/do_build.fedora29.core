#! /bin/csh

#
# build core in fedora29 image
#

cd ~/git/lrose-core/build/packages/fedora

docker image rm fedora29:build

docker build \
    --tag fedora29:build \
    --file Dockerfile.fedora29.build.core .

