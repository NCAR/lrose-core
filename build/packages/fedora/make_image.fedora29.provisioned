#! /bin/csh

#
# build docker image for fedora, with required packages
#

cd ~/git/lrose-core/build/packages/fedora

docker build \
    --tag fedora29:provisioned \
    --file Dockerfile.fedora29.provisioned .


