#! /bin/csh

#
# build blaze in centos6 image
#

cd ~/git/lrose-core/build/packaging/centos

docker image rm centos6:blaze

docker build \
    --tag centos6:blaze \
    --file Dockerfile.centos6.build.blaze .

