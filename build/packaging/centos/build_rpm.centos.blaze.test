#! /bin/csh

#
# build docker image for centos, with required packages
#

cd ~/git/lrose-core/build/packaging/centos

docker build \
    -t centos:blaze-test \
    -f Dockerfile.centos.blaze.test .


