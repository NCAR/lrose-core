#! /bin/csh

#
# in clean docker image,
# install rpm and test that the apps work
#

cd ~/git/lrose-core/build/packaging/centos

# remove any old image

docker rm image centos6:installed

# make rpms available to container

set rpmDir = /tmp/centos6-blaze/rpms
mkdir -p $rpmDir

# install the rpms in a container

docker build \
    -v ${rpmDir}:/rpms \
    --tag centos6:installed \
    --file Dockerfile.centos6.install_and_test .

