###############################################################
# build lrose-blaze on fedora28
# start with provisioned image
# perform the build

FROM fedora28:provisioned

# get build script

RUN \
    cd ${HOME}; \
    mkdir -p git; cd git; \
    git clone https://github.com/NCAR/lrose-core

# run the build script

RUN \
    cd ${HOME}/git/lrose-core/build; \
    ./checkout_and_build_auto.py --package lrose-blaze \
    --prefix /usr/local/lrose --scripts --useSystemNetcdf \
    --buildDir /tmp/lrose_build --logDir /tmp/build_logs

# clean up

RUN \
    /bin/rm -rf /tmp/lrose_build

