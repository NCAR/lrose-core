#! /bin/csh

#
# build docker image for ubuntu, with required packages
#

cd ~/git/lrose-core/build/packaging/ubuntu

docker build \
    --tag ubuntu16:provisioned \
    --file Dockerfile.ubuntu16.provisioned .


