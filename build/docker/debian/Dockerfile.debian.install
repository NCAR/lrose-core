#===================================================
# Install lrose pkg in clean container
# using the .deb pkg file

ARG OS_TYPE=centos

# install the .deb file

RUN /scripts/perform_install.debian -t ${OS_TYPE}

#####################################################
