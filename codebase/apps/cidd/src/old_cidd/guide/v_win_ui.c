/*
 * v_win_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `v_win.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include <group.h>
#include "v_win_ui.h"

/*
 * Create object `v_field_mu' in the specified instance.
 */
Xv_opaque
v_win_v_field_mu_create(ip, owner)
	caddr_t		ip;
	Xv_opaque	owner;
{
	extern Menu		v_field_mu_gen_proc();
	Xv_opaque	obj;
	
	obj = xv_create(XV_NULL, MENU_CHOICE_MENU,
		XV_KEY_DATA, INSTANCE, ip,
		MENU_GEN_PROC, v_field_mu_gen_proc,
		MENU_TITLE_ITEM, "Data Field",
		NULL);
	return obj;
}

/*
 * Initialize an instance of object `v_win_pu'.
 */
v_win_v_win_pu_objects *
v_win_v_win_pu_objects_initialize(ip, owner)
	v_win_v_win_pu_objects	*ip;
	Xv_opaque	owner;
{
	if (!ip && !(ip = (v_win_v_win_pu_objects *) calloc(1, sizeof (v_win_v_win_pu_objects))))
		return (v_win_v_win_pu_objects *) NULL;
	if (!ip->v_win_pu)
		ip->v_win_pu = v_win_v_win_pu_v_win_pu_create(ip, owner);
	if (!ip->canvas1)
		ip->canvas1 = v_win_v_win_pu_canvas1_create(ip, ip->v_win_pu);
	return ip;
}

/*
 * Create object `v_win_pu' in the specified instance.
 */
Xv_opaque
v_win_v_win_pu_v_win_pu_create(ip, owner)
	v_win_v_win_pu_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 435,
		XV_HEIGHT, 154,
		XV_LABEL, "RHI",
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, FALSE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, FALSE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `canvas1' in the specified instance.
 */
Xv_opaque
v_win_v_win_pu_canvas1_create(ip, owner)
	v_win_v_win_pu_objects	*ip;
	Xv_opaque	owner;
{
	extern Notify_value	v_can_events();
	extern void	v_can_repaint();
	extern Xv_opaque	v_win_v_field_mu_create();
	Xv_opaque	obj;
	
	obj = xv_create(owner, CANVAS,
		XV_KEY_DATA, INSTANCE, ip,
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		CANVAS_REPAINT_PROC, v_can_repaint,
		CANVAS_X_PAINT_WINDOW, TRUE,
		NULL);
	xv_set(canvas_paint_window(obj), WIN_MENU, v_win_v_field_mu_create((caddr_t) ip, ip->v_win_pu), NULL);
	xv_set(canvas_paint_window(obj), WIN_CONSUME_EVENTS,
		WIN_MOUSE_BUTTONS,
		LOC_MOVE,
		LOC_DRAG,
		LOC_WINENTER,
		LOC_WINEXIT,
		WIN_ASCII_EVENTS,
		WIN_LEFT_KEYS,
		WIN_RIGHT_KEYS,
		WIN_TOP_KEYS,
		NULL, NULL);
	notify_interpose_event_func(canvas_paint_window(obj),
		(Notify_func) v_can_events, NOTIFY_SAFE);
	/*
	 * This line is here for backwards compatibility. It will be
	 * removed for the next release.
	 */
	xv_set(canvas_paint_window(obj), XV_KEY_DATA, INSTANCE, ip, NULL);
	return obj;
}

