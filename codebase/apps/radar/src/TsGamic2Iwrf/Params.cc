// *=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*
// ** Copyright UCAR (c) 1992 - 2016
// ** University Corporation for Atmospheric Research(UCAR)
// ** National Center for Atmospheric Research(NCAR)
// ** Boulder, Colorado, USA
// *=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*
////////////////////////////////////////////
// Params.cc
//
// TDRP C++ code file for class 'Params'.
//
// Code for program TsGamic2Iwrf
//
// This file has been automatically
// generated by TDRP, do not modify.
//
/////////////////////////////////////////////

/**
 *
 * @file Params.cc
 *
 * @class Params
 *
 * This class is automatically generated by the Table
 * Driven Runtime Parameters (TDRP) system
 *
 * @note Source is automatically generated from
 *       paramdef file at compile time, do not modify
 *       since modifications will be overwritten.
 *
 *
 * @author Automatically generated
 *
 */
using namespace std;

#include "Params.hh"
#include <cstring>

  ////////////////////////////////////////////
  // Default constructor
  //

  Params::Params()

  {

    // zero out table

    memset(_table, 0, sizeof(_table));

    // zero out members

    memset(&_start_, 0, &_end_ - &_start_);

    // class name

    _className = "Params";

    // initialize table

    _init();

    // set members

    tdrpTable2User(_table, &_start_);

    _exitDeferred = false;

  }

  ////////////////////////////////////////////
  // Copy constructor
  //

  Params::Params(const Params& source)

  {

    // sync the source object

    source.sync();

    // zero out table

    memset(_table, 0, sizeof(_table));

    // zero out members

    memset(&_start_, 0, &_end_ - &_start_);

    // class name

    _className = "Params";

    // copy table

    tdrpCopyTable((TDRPtable *) source._table, _table);

    // set members

    tdrpTable2User(_table, &_start_);

    _exitDeferred = false;

  }

  ////////////////////////////////////////////
  // Destructor
  //

  Params::~Params()

  {

    // free up

    freeAll();

  }

  ////////////////////////////////////////////
  // Assignment
  //

  void Params::operator=(const Params& other)

  {

    // sync the other object

    other.sync();

    // free up any existing memory

    freeAll();

    // zero out table

    memset(_table, 0, sizeof(_table));

    // zero out members

    memset(&_start_, 0, &_end_ - &_start_);

    // copy table

    tdrpCopyTable((TDRPtable *) other._table, _table);

    // set members

    tdrpTable2User(_table, &_start_);

    _exitDeferred = other._exitDeferred;

  }

  ////////////////////////////////////////////
  // loadFromArgs()
  //
  // Loads up TDRP using the command line args.
  //
  // Check usage() for command line actions associated with
  // this function.
  //
  //   argc, argv: command line args
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   char **params_path_p:
  //     If this is non-NULL, it is set to point to the path
  //     of the params file used.
  //
  //   bool defer_exit: normally, if the command args contain a 
  //      print or check request, this function will call exit().
  //      If defer_exit is set, such an exit is deferred and the
  //      private member _exitDeferred is set.
  //      Use exidDeferred() to test this flag.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadFromArgs(int argc, char **argv,
                           char **override_list,
                           char **params_path_p,
                           bool defer_exit)
  {
    int exit_deferred;
    if (_tdrpLoadFromArgs(argc, argv,
                          _table, &_start_,
                          override_list, params_path_p,
                          _className,
                          defer_exit, &exit_deferred)) {
      return (-1);
    } else {
      if (exit_deferred) {
        _exitDeferred = true;
      }
      return (0);
    }
  }

  ////////////////////////////////////////////
  // loadApplyArgs()
  //
  // Loads up TDRP using the params path passed in, and applies
  // the command line args for printing and checking.
  //
  // Check usage() for command line actions associated with
  // this function.
  //
  //   const char *param_file_path: the parameter file to be read in
  //
  //   argc, argv: command line args
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   bool defer_exit: normally, if the command args contain a 
  //      print or check request, this function will call exit().
  //      If defer_exit is set, such an exit is deferred and the
  //      private member _exitDeferred is set.
  //      Use exidDeferred() to test this flag.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadApplyArgs(const char *params_path,
                            int argc, char **argv,
                            char **override_list,
                            bool defer_exit)
  {
    int exit_deferred;
    if (tdrpLoadApplyArgs(params_path, argc, argv,
                          _table, &_start_,
                          override_list,
                          _className,
                          defer_exit, &exit_deferred)) {
      return (-1);
    } else {
      if (exit_deferred) {
        _exitDeferred = true;
      }
      return (0);
    }
  }

  ////////////////////////////////////////////
  // isArgValid()
  // 
  // Check if a command line arg is a valid TDRP arg.
  //

  bool Params::isArgValid(const char *arg)
  {
    return (tdrpIsArgValid(arg));
  }

  ////////////////////////////////////////////
  // load()
  //
  // Loads up TDRP for a given class.
  //
  // This version of load gives the programmer the option to load
  // up more than one class for a single application. It is a
  // lower-level routine than loadFromArgs, and hence more
  // flexible, but the programmer must do more work.
  //
  //   const char *param_file_path: the parameter file to be read in.
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   expand_env: flag to control environment variable
  //               expansion during tokenization.
  //               If TRUE, environment expansion is set on.
  //               If FALSE, environment expansion is set off.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::load(const char *param_file_path,
                   char **override_list,
                   int expand_env, int debug)
  {
    if (tdrpLoad(param_file_path,
                 _table, &_start_,
                 override_list,
                 expand_env, debug)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // loadFromBuf()
  //
  // Loads up TDRP for a given class.
  //
  // This version of load gives the programmer the option to
  // load up more than one module for a single application,
  // using buffers which have been read from a specified source.
  //
  //   const char *param_source_str: a string which describes the
  //     source of the parameter information. It is used for
  //     error reporting only.
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   const char *inbuf: the input buffer
  //
  //   int inlen: length of the input buffer
  //
  //   int start_line_num: the line number in the source which
  //     corresponds to the start of the buffer.
  //
  //   expand_env: flag to control environment variable
  //               expansion during tokenization.
  //               If TRUE, environment expansion is set on.
  //               If FALSE, environment expansion is set off.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadFromBuf(const char *param_source_str,
                          char **override_list,
                          const char *inbuf, int inlen,
                          int start_line_num,
                          int expand_env, int debug)
  {
    if (tdrpLoadFromBuf(param_source_str,
                        _table, &_start_,
                        override_list,
                        inbuf, inlen, start_line_num,
                        expand_env, debug)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // loadDefaults()
  //
  // Loads up default params for a given class.
  //
  // See load() for more detailed info.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadDefaults(int expand_env)
  {
    if (tdrpLoad(NULL,
                 _table, &_start_,
                 NULL, expand_env, FALSE)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // sync()
  //
  // Syncs the user struct data back into the parameter table,
  // in preparation for printing.
  //
  // This function alters the table in a consistent manner.
  // Therefore it can be regarded as const.
  //

  void Params::sync(void) const
  {
    tdrpUser2Table(_table, (char *) &_start_);
  }

  ////////////////////////////////////////////
  // print()
  // 
  // Print params file
  //
  // The modes supported are:
  //
  //   PRINT_SHORT:   main comments only, no help or descriptions
  //                  structs and arrays on a single line
  //   PRINT_NORM:    short + descriptions and help
  //   PRINT_LONG:    norm  + arrays and structs expanded
  //   PRINT_VERBOSE: long  + private params included
  //

  void Params::print(FILE *out, tdrp_print_mode_t mode)
  {
    tdrpPrint(out, _table, _className, mode);
  }

  ////////////////////////////////////////////
  // checkAllSet()
  //
  // Return TRUE if all set, FALSE if not.
  //
  // If out is non-NULL, prints out warning messages for those
  // parameters which are not set.
  //

  int Params::checkAllSet(FILE *out)
  {
    return (tdrpCheckAllSet(out, _table, &_start_));
  }

  //////////////////////////////////////////////////////////////
  // checkIsSet()
  //
  // Return TRUE if parameter is set, FALSE if not.
  //
  //

  int Params::checkIsSet(const char *paramName)
  {
    return (tdrpCheckIsSet(paramName, _table, &_start_));
  }

  ////////////////////////////////////////////
  // freeAll()
  //
  // Frees up all TDRP dynamic memory.
  //

  void Params::freeAll(void)
  {
    tdrpFreeAll(_table, &_start_);
  }

  ////////////////////////////////////////////
  // usage()
  //
  // Prints out usage message for TDRP args as passed
  // in to loadFromArgs().
  //

  void Params::usage(ostream &out)
  {
    out << "TDRP args: [options as below]\n"
        << "   [ -params/--params path ] specify params file path\n"
        << "   [ -check_params/--check_params] check which params are not set\n"
        << "   [ -print_params/--print_params [mode]] print parameters\n"
        << "     using following modes, default mode is 'norm'\n"
        << "       short:   main comments only, no help or descr\n"
        << "                structs and arrays on a single line\n"
        << "       norm:    short + descriptions and help\n"
        << "       long:    norm  + arrays and structs expanded\n"
        << "       verbose: long  + private params included\n"
        << "       short_expand:   short with env vars expanded\n"
        << "       norm_expand:    norm with env vars expanded\n"
        << "       long_expand:    long with env vars expanded\n"
        << "       verbose_expand: verbose with env vars expanded\n"
        << "   [ -tdrp_debug] debugging prints for tdrp\n"
        << "   [ -tdrp_usage] print this usage\n";
  }

  ////////////////////////////////////////////
  // arrayRealloc()
  //
  // Realloc 1D array.
  //
  // If size is increased, the values from the last array 
  // entry is copied into the new space.
  //
  // Returns 0 on success, -1 on error.
  //

  int Params::arrayRealloc(const char *param_name, int new_array_n)
  {
    if (tdrpArrayRealloc(_table, &_start_,
                         param_name, new_array_n)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // array2DRealloc()
  //
  // Realloc 2D array.
  //
  // If size is increased, the values from the last array 
  // entry is copied into the new space.
  //
  // Returns 0 on success, -1 on error.
  //

  int Params::array2DRealloc(const char *param_name,
                             int new_array_n1,
                             int new_array_n2)
  {
    if (tdrpArray2DRealloc(_table, &_start_, param_name,
                           new_array_n1, new_array_n2)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // _init()
  //
  // Class table initialization function.
  //
  //

  void Params::_init()

  {

    TDRPtable *tt = _table;

    // Parameter 'Comment 0'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 0");
    tt->comment_hdr = tdrpStrDup("TsGamic2Iwrf reads pulse IQ data in GAMIC format and converts to IWRF format.");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'Comment 1'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 1");
    tt->comment_hdr = tdrpStrDup("DEBUGGING AND PROCESS CONTROL.");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'debug'
    // ctype is '_debug_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("debug");
    tt->descr = tdrpStrDup("Debug option");
    tt->help = tdrpStrDup("If set, debug messages will be printed appropriately");
    tt->val_offset = (char *) &debug - &_start_;
    tt->enum_def.name = tdrpStrDup("debug_t");
    tt->enum_def.nfields = 4;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
      tt->enum_def.fields[0].val = DEBUG_OFF;
      tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_NORM");
      tt->enum_def.fields[1].val = DEBUG_NORM;
      tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_VERBOSE");
      tt->enum_def.fields[2].val = DEBUG_VERBOSE;
      tt->enum_def.fields[3].name = tdrpStrDup("DEBUG_EXTRA");
      tt->enum_def.fields[3].val = DEBUG_EXTRA;
    tt->single_val.e = DEBUG_OFF;
    tt++;
    
    // Parameter 'instance'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("instance");
    tt->descr = tdrpStrDup("Process instance");
    tt->help = tdrpStrDup("Used for registration with procmap.");
    tt->val_offset = (char *) &instance - &_start_;
    tt->single_val.s = tdrpStrDup("test");
    tt++;
    
    // Parameter 'register_with_procmap'
    // ctype is 'tdrp_bool_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = BOOL_TYPE;
    tt->param_name = tdrpStrDup("register_with_procmap");
    tt->descr = tdrpStrDup("Option to register with the process mapper.");
    tt->help = tdrpStrDup("If true, this application will try to register with procmap once per minute. (If unable to do so, no error occurs.)");
    tt->val_offset = (char *) &register_with_procmap - &_start_;
    tt->single_val.b = pFALSE;
    tt++;
    
    // Parameter 'Comment 2'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 2");
    tt->comment_hdr = tdrpStrDup("INPUT DATA");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'mode'
    // ctype is '_mode_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("mode");
    tt->descr = tdrpStrDup("Operating mode");
    tt->help = tdrpStrDup("In REALTIME mode, the program waits for a new input file to process. In ARCHIVE mode, it moves through the list of file names specified on the command line.");
    tt->val_offset = (char *) &mode - &_start_;
    tt->enum_def.name = tdrpStrDup("mode_t");
    tt->enum_def.nfields = 2;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("ARCHIVE");
      tt->enum_def.fields[0].val = ARCHIVE;
      tt->enum_def.fields[1].name = tdrpStrDup("REALTIME");
      tt->enum_def.fields[1].val = REALTIME;
    tt->single_val.e = ARCHIVE;
    tt++;
    
    // Parameter 'input_dir'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("input_dir");
    tt->descr = tdrpStrDup("Directory for IQ data files");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &input_dir - &_start_;
    tt->single_val.s = tdrpStrDup("/tmp/input");
    tt++;
    
    // Parameter 'Comment 3'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 3");
    tt->comment_hdr = tdrpStrDup("SET RADAR INFO");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'radar_name'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("radar_name");
    tt->descr = tdrpStrDup("Name of radar");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &radar_name - &_start_;
    tt->single_val.s = tdrpStrDup("Hohnpeissenberg");
    tt++;
    
    // Parameter 'site_name'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("site_name");
    tt->descr = tdrpStrDup("Name of radar site");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &site_name - &_start_;
    tt->single_val.s = tdrpStrDup("Hohnpeissenberg");
    tt++;
    
    // Parameter 'radar_latitude'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("radar_latitude");
    tt->descr = tdrpStrDup("Latitude of radar.");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &radar_latitude - &_start_;
    tt->single_val.d = 47.8015;
    tt++;
    
    // Parameter 'radar_longitude'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("radar_longitude");
    tt->descr = tdrpStrDup("Longitude of radar.");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &radar_longitude - &_start_;
    tt->single_val.d = 11.0093;
    tt++;
    
    // Parameter 'radar_altitude_m'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("radar_altitude_m");
    tt->descr = tdrpStrDup("Altitude of radar dish (m).");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &radar_altitude_m - &_start_;
    tt->single_val.d = 1006.2;
    tt++;
    
    // Parameter 'antenna_gain_db'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("antenna_gain_db");
    tt->descr = tdrpStrDup("Gain of antenna (dB).");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &antenna_gain_db - &_start_;
    tt->single_val.d = 45.9;
    tt++;
    
    // Parameter 'xmit_power_dbm_h'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("xmit_power_dbm_h");
    tt->descr = tdrpStrDup("Nominal transmit power, H channel (dBm).");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &xmit_power_dbm_h - &_start_;
    tt->single_val.d = 85;
    tt++;
    
    // Parameter 'xmit_power_dbm_v'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("xmit_power_dbm_v");
    tt->descr = tdrpStrDup("Nominal transmit power, V channel (dBm).");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &xmit_power_dbm_v - &_start_;
    tt->single_val.d = 85;
    tt++;
    
    // Parameter 'xmit_rcv_mode'
    // ctype is '_xmit_rcv_mode_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("xmit_rcv_mode");
    tt->descr = tdrpStrDup("Transmit-receive mode for radar.");
    tt->help = tdrpStrDup("  SINGLE_POL: Single polarization (NEXRAD)\n\n  DP_ALT_HV_CO_ONLY: Dual pol, alternating transmission, copolar receiver only (CP2 SBand)\n\n  DP_ALT_HV_CO_CROSS: Dual pol, alternating transmission, co-polar and cross-polar receivers (SPOL with Mitch Switch and receiver in switching mode, CHILL)\n\n  DP_ALT_HV_FIXED_HV: Dual pol, alternating transmission, fixed H and V receivers (SPOL with Mitch Switch and receivers in fixed mode)\n\n  DP_SIM_HV_FIXED_HV: Dual pol, simultaneous transmission, fixed H and V receivers (NEXRAD upgrade, SPOL with T and receivers in fixed mode)\n\n  DP_SIM_HV_SWITCHED_HV: Dual pol, simultaneous transmission, switching H and V receivers (SPOL with T and receivers in switching mode)\n\n  DP_H_ONLY_FIXED_HV: Dual pol, H transmission, fixed H and V receivers (CP2 X band)\n\n  DP_V_ONLY_FIXED_HV: Dual pol, V transmission, fixed H and V receivers (CP2 X band)\n\n  DP_ALT_HHVV_FIXED_HV: Dual pol, alternating transmission, pulsing HHVV sequence, fixed receiver chain (HCR)\n\n");
    tt->val_offset = (char *) &xmit_rcv_mode - &_start_;
    tt->enum_def.name = tdrpStrDup("xmit_rcv_mode_t");
    tt->enum_def.nfields = 10;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("XMIT_RCV_UNKNOWN");
      tt->enum_def.fields[0].val = XMIT_RCV_UNKNOWN;
      tt->enum_def.fields[1].name = tdrpStrDup("SINGLE_POL");
      tt->enum_def.fields[1].val = SINGLE_POL;
      tt->enum_def.fields[2].name = tdrpStrDup("DP_ALT_HV_CO_ONLY");
      tt->enum_def.fields[2].val = DP_ALT_HV_CO_ONLY;
      tt->enum_def.fields[3].name = tdrpStrDup("DP_ALT_HV_CO_CROSS");
      tt->enum_def.fields[3].val = DP_ALT_HV_CO_CROSS;
      tt->enum_def.fields[4].name = tdrpStrDup("DP_ALT_HV_FIXED_HV");
      tt->enum_def.fields[4].val = DP_ALT_HV_FIXED_HV;
      tt->enum_def.fields[5].name = tdrpStrDup("DP_SIM_HV_FIXED_HV");
      tt->enum_def.fields[5].val = DP_SIM_HV_FIXED_HV;
      tt->enum_def.fields[6].name = tdrpStrDup("DP_SIM_HV_SWITCHED_HV");
      tt->enum_def.fields[6].val = DP_SIM_HV_SWITCHED_HV;
      tt->enum_def.fields[7].name = tdrpStrDup("DP_H_ONLY_FIXED_HV");
      tt->enum_def.fields[7].val = DP_H_ONLY_FIXED_HV;
      tt->enum_def.fields[8].name = tdrpStrDup("DP_V_ONLY_FIXED_HV");
      tt->enum_def.fields[8].val = DP_V_ONLY_FIXED_HV;
      tt->enum_def.fields[9].name = tdrpStrDup("DP_ALT_HHVV_FIXED_HV");
      tt->enum_def.fields[9].val = DP_ALT_HHVV_FIXED_HV;
    tt->single_val.e = SINGLE_POL;
    tt++;
    
    // Parameter 'scan_mode'
    // ctype is '_scan_mode_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("scan_mode");
    tt->descr = tdrpStrDup("Scan mode for radar.");
    tt->help = tdrpStrDup("Set to the relevant scan mode\n\nSCAN_MODE_SECTOR: sector scan mode\n\nSCAN_MODE_COPLANE: co-plane dual doppler mode\n\nSCAN_MODE_RHI: range height vertical scanning mode\n\nSCAN_MODE_VERTICAL_POINTING: vertical pointing for calibration\n\nSCAN_MODE_IDLE: between scans\n\nSCAN_MODE_AZ_SUR_360: 360-degree azimuth mode - surveillance\n\nSCAN_MODE_EL_SUR_360: 360-degree elevation mode - eg Eldora\n\nSCAN_MODE_SUNSCAN: scanning the sun for calibrations\n\nSCAN_MODE_POINTING: fixed pointing\n\nSCAN_MODE_FOLLOW_VEHICLE: follow target vehicle\n\nSCAN_MODE_EL_SURV: elevation surveillance (ELDORA)\n\nSCAN_MODE_MANPPI: Manual PPI mode (elevation does\n\nSCAN_MODE_MANRHI: Manual RHI mode (azimuth does\n\nSCAN_MODE_SUNSCAN_RHI: sunscan in RHI mode\n\n");
    tt->val_offset = (char *) &scan_mode - &_start_;
    tt->enum_def.name = tdrpStrDup("scan_mode_t");
    tt->enum_def.nfields = 15;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("SCAN_MODE_UNKNOWN");
      tt->enum_def.fields[0].val = SCAN_MODE_UNKNOWN;
      tt->enum_def.fields[1].name = tdrpStrDup("SCAN_MODE_SECTOR");
      tt->enum_def.fields[1].val = SCAN_MODE_SECTOR;
      tt->enum_def.fields[2].name = tdrpStrDup("SCAN_MODE_COPLANE");
      tt->enum_def.fields[2].val = SCAN_MODE_COPLANE;
      tt->enum_def.fields[3].name = tdrpStrDup("SCAN_MODE_RHI");
      tt->enum_def.fields[3].val = SCAN_MODE_RHI;
      tt->enum_def.fields[4].name = tdrpStrDup("SCAN_MODE_VERTICAL_POINTING");
      tt->enum_def.fields[4].val = SCAN_MODE_VERTICAL_POINTING;
      tt->enum_def.fields[5].name = tdrpStrDup("SCAN_MODE_IDLE");
      tt->enum_def.fields[5].val = SCAN_MODE_IDLE;
      tt->enum_def.fields[6].name = tdrpStrDup("SCAN_MODE_AZ_SUR_360");
      tt->enum_def.fields[6].val = SCAN_MODE_AZ_SUR_360;
      tt->enum_def.fields[7].name = tdrpStrDup("SCAN_MODE_EL_SUR_360");
      tt->enum_def.fields[7].val = SCAN_MODE_EL_SUR_360;
      tt->enum_def.fields[8].name = tdrpStrDup("SCAN_MODE_SUNSCAN");
      tt->enum_def.fields[8].val = SCAN_MODE_SUNSCAN;
      tt->enum_def.fields[9].name = tdrpStrDup("SCAN_MODE_POINTING");
      tt->enum_def.fields[9].val = SCAN_MODE_POINTING;
      tt->enum_def.fields[10].name = tdrpStrDup("SCAN_MODE_FOLLOW_VEHICLE");
      tt->enum_def.fields[10].val = SCAN_MODE_FOLLOW_VEHICLE;
      tt->enum_def.fields[11].name = tdrpStrDup("SCAN_MODE_EL_SURV");
      tt->enum_def.fields[11].val = SCAN_MODE_EL_SURV;
      tt->enum_def.fields[12].name = tdrpStrDup("SCAN_MODE_MANPPI");
      tt->enum_def.fields[12].val = SCAN_MODE_MANPPI;
      tt->enum_def.fields[13].name = tdrpStrDup("SCAN_MODE_MANRHI");
      tt->enum_def.fields[13].val = SCAN_MODE_MANRHI;
      tt->enum_def.fields[14].name = tdrpStrDup("SCAN_MODE_SUNSCAN_RHI");
      tt->enum_def.fields[14].val = SCAN_MODE_SUNSCAN_RHI;
    tt->single_val.e = SCAN_MODE_AZ_SUR_360;
    tt++;
    
    // Parameter 'Comment 4'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 4");
    tt->comment_hdr = tdrpStrDup("PULSE WIDTH DECODING");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'pulse_width_lookup'
    // ctype is '_pulse_width_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRUCT_TYPE;
    tt->param_name = tdrpStrDup("pulse_width_lookup");
    tt->descr = tdrpStrDup("Lookup table for pulse widths");
    tt->help = tdrpStrDup("The data only contains the pulse width index. This lookup table allows you to decode the pulse width from the index.");
    tt->array_offset = (char *) &_pulse_width_lookup - &_start_;
    tt->array_n_offset = (char *) &pulse_width_lookup_n - &_start_;
    tt->is_array = TRUE;
    tt->array_len_fixed = FALSE;
    tt->array_elem_size = sizeof(pulse_width_t);
    tt->array_n = 2;
    tt->struct_def.name = tdrpStrDup("pulse_width_t");
    tt->struct_def.nfields = 2;
    tt->struct_def.fields = (struct_field_t *)
        tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
      tt->struct_def.fields[0].ftype = tdrpStrDup("int");
      tt->struct_def.fields[0].fname = tdrpStrDup("index");
      tt->struct_def.fields[0].ptype = INT_TYPE;
      tt->struct_def.fields[0].rel_offset = 
        (char *) &_pulse_width_lookup->index - (char *) _pulse_width_lookup;
      tt->struct_def.fields[1].ftype = tdrpStrDup("double");
      tt->struct_def.fields[1].fname = tdrpStrDup("pulse_width_us");
      tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
      tt->struct_def.fields[1].rel_offset = 
        (char *) &_pulse_width_lookup->pulse_width_us - (char *) _pulse_width_lookup;
    tt->n_struct_vals = 4;
    tt->struct_vals = (tdrpVal_t *)
        tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
      tt->struct_vals[0].i = 0;
      tt->struct_vals[1].d = 0.4;
      tt->struct_vals[2].i = 1;
      tt->struct_vals[3].d = 0.8;
    tt++;
    
    // Parameter 'Comment 5'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 5");
    tt->comment_hdr = tdrpStrDup("SCALING FOR NOMINAL RECEIVER GAIN");
    tt->comment_text = tdrpStrDup("The raw I/Q data should be scaled to provide powers in the correct range.");
    tt++;
    
    // Parameter 'rx_gain_nominal_db_h'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("rx_gain_nominal_db_h");
    tt->descr = tdrpStrDup("Nominal receiver gain in H channel (dB).");
    tt->help = tdrpStrDup("The IQ data will be scaled based on this nominal gain");
    tt->val_offset = (char *) &rx_gain_nominal_db_h - &_start_;
    tt->single_val.d = 107;
    tt++;
    
    // Parameter 'rx_gain_nominal_db_v'
    // ctype is 'double'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = DOUBLE_TYPE;
    tt->param_name = tdrpStrDup("rx_gain_nominal_db_v");
    tt->descr = tdrpStrDup("Nominal receiver gain in V channel (dB).");
    tt->help = tdrpStrDup("The IQ data will be scaled based on this nominal gain");
    tt->val_offset = (char *) &rx_gain_nominal_db_v - &_start_;
    tt->single_val.d = 107;
    tt++;
    
    // Parameter 'Comment 6'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 6");
    tt->comment_hdr = tdrpStrDup("OUTPUT FILES");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'output_dir'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("output_dir");
    tt->descr = tdrpStrDup("Output directory path.");
    tt->help = tdrpStrDup("Files are written to this directory.");
    tt->val_offset = (char *) &output_dir - &_start_;
    tt->single_val.s = tdrpStrDup("/tmp/outdir");
    tt++;
    
    // Parameter 'add_scan_mode_to_file_name'
    // ctype is 'tdrp_bool_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = BOOL_TYPE;
    tt->param_name = tdrpStrDup("add_scan_mode_to_file_name");
    tt->descr = tdrpStrDup("Option to add a string indicating the scan mode to the file name.");
    tt->help = tdrpStrDup("");
    tt->val_offset = (char *) &add_scan_mode_to_file_name - &_start_;
    tt->single_val.b = pTRUE;
    tt++;
    
    // Parameter 'output_format'
    // ctype is '_output_format_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("output_format");
    tt->descr = tdrpStrDup("Format of the output files");
    tt->help = tdrpStrDup("IWRF: stream of binary structs and IQ data following the IWRF (CSU-CHILL/NCAR Integrated Weather Radar Facility) format. TSACHIVE: Sigmet time series archive format");
    tt->val_offset = (char *) &output_format - &_start_;
    tt->enum_def.name = tdrpStrDup("output_format_t");
    tt->enum_def.nfields = 2;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("FORMAT_IWRF");
      tt->enum_def.fields[0].val = FORMAT_IWRF;
      tt->enum_def.fields[1].name = tdrpStrDup("FORMAT_TSARCHIVE");
      tt->enum_def.fields[1].val = FORMAT_TSARCHIVE;
    tt->single_val.e = FORMAT_IWRF;
    tt++;
    
    // Parameter 'output_packing'
    // ctype is '_output_packing_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("output_packing");
    tt->descr = tdrpStrDup("Packing of the IQ data in the output files");
    tt->help = tdrpStrDup("ASIS: as it was read. FL32: 32-bit floating point. SCALED_SI16: scaled signed 16-bit integers. DBM_PHASE_SI16: signed 16-bit integers representing power in dBM and phase in deg. SIGMET_SI16: Sigmet 16-bit floating point packing as in the RVP8.");
    tt->val_offset = (char *) &output_packing - &_start_;
    tt->enum_def.name = tdrpStrDup("output_packing_t");
    tt->enum_def.nfields = 5;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("PACKING_ASIS");
      tt->enum_def.fields[0].val = PACKING_ASIS;
      tt->enum_def.fields[1].name = tdrpStrDup("PACKING_FL32");
      tt->enum_def.fields[1].val = PACKING_FL32;
      tt->enum_def.fields[2].name = tdrpStrDup("PACKING_SCALED_SI16");
      tt->enum_def.fields[2].val = PACKING_SCALED_SI16;
      tt->enum_def.fields[3].name = tdrpStrDup("PACKING_DBM_PHASE_SI16");
      tt->enum_def.fields[3].val = PACKING_DBM_PHASE_SI16;
      tt->enum_def.fields[4].name = tdrpStrDup("PACKING_SIGMET_FL16");
      tt->enum_def.fields[4].val = PACKING_SIGMET_FL16;
    tt->single_val.e = PACKING_FL32;
    tt++;
    
    // trailing entry has param_name set to NULL
    
    tt->param_name = NULL;
    
    return;
  
  }
