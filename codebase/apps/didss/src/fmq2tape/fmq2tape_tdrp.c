/*******************************************
 * fmq2tape_tdrp.c
 *
 * TDRP C code file 'fmq2tape' module.
 *
 * Code for program fmq2tape
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "fmq2tape_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[8];
static fmq2tape_tdrp_struct *Params;
static char *Module = "fmq2tape";

/*************************************************************
 * fmq2tape_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   fmq2tape_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int fmq2tape_tdrp_load_from_args(int argc, char **argv,
                         fmq2tape_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  fmq2tape_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * fmq2tape_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than fmq2tape_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   fmq2tape_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int fmq2tape_tdrp_load(char *param_file_path,
               fmq2tape_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  fmq2tape_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * fmq2tape_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See fmq2tape_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int fmq2tape_tdrp_load_defaults(fmq2tape_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  fmq2tape_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * fmq2tape_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void fmq2tape_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * fmq2tape_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void fmq2tape_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * fmq2tape_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int fmq2tape_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * fmq2tape_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int fmq2tape_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * fmq2tape_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void fmq2tape_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * fmq2tape_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int fmq2tape_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * fmq2tape_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int fmq2tape_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * fmq2tape_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *fmq2tape_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * fmq2tape_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *fmq2tape_tdrp_init(fmq2tape_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  fmq2tape_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(fmq2tape_tdrp_struct));
  params->struct_size = sizeof(fmq2tape_tdrp_struct);

  /* Parameter 'debug' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("Debug option.");
  tt->help = tdrpStrDup("Print debug messages.");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("instance");
  tt->descr = tdrpStrDup("Process instance.");
  tt->help = tdrpStrDup(" ");
  tt->val_offset = (char *) &(pp.instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("Malloc debug level");
  tt->help = tdrpStrDup("0 - none, 1 - corruption checking, 2 - records all malloc blocks and checks, 3 - printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'fmq_path' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("fmq_path");
  tt->descr = tdrpStrDup("FMQ path.");
  tt->help = tdrpStrDup("Directory for FMQ files.");
  tt->val_offset = (char *) &(pp.fmq_path) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("fmq_dir");
  tt++;
  
  /* Parameter 'max_block_size' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("max_block_size");
  tt->descr = tdrpStrDup("Maximum block size.");
  tt->help = tdrpStrDup("Maximum number of bytes for block to be written to tape.");
  tt->val_offset = (char *) &(pp.max_block_size) - (char *) &pp;
  tt->single_val.l = 65536;
  tt++;
  
  /* Parameter 'max_message_per_block' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("max_message_per_block");
  tt->descr = tdrpStrDup("Maxinum number of messages.");
  tt->help = tdrpStrDup("Maxinum number of messages per block to be written to tape.");
  tt->val_offset = (char *) &(pp.max_message_per_block) - (char *) &pp;
  tt->single_val.l = 10;
  tt++;
  
  /* Parameter 'tape_device_name' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("tape_device_name");
  tt->descr = tdrpStrDup("Tape device name.");
  tt->help = tdrpStrDup("Name of tape device to which the fmq will be written.");
  tt->val_offset = (char *) &(pp.tape_device_name) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("/dev/tape");
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
