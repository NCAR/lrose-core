/*******************************************
 * wsi_ingest_tdrp.c
 *
 * TDRP C code file 'wsi_ingest' module.
 *
 * Code for program wsi_ingest
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "wsi_ingest_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[7];
static wsi_ingest_tdrp_struct *Params;
static char *Module = "wsi_ingest";

/*************************************************************
 * wsi_ingest_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   wsi_ingest_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int wsi_ingest_tdrp_load_from_args(int argc, char **argv,
                         wsi_ingest_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  wsi_ingest_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * wsi_ingest_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than wsi_ingest_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   wsi_ingest_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int wsi_ingest_tdrp_load(char *param_file_path,
               wsi_ingest_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  wsi_ingest_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * wsi_ingest_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See wsi_ingest_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int wsi_ingest_tdrp_load_defaults(wsi_ingest_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  wsi_ingest_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * wsi_ingest_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void wsi_ingest_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * wsi_ingest_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void wsi_ingest_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * wsi_ingest_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int wsi_ingest_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * wsi_ingest_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int wsi_ingest_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * wsi_ingest_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void wsi_ingest_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * wsi_ingest_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int wsi_ingest_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * wsi_ingest_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int wsi_ingest_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * wsi_ingest_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *wsi_ingest_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * wsi_ingest_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *wsi_ingest_tdrp_init(wsi_ingest_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  wsi_ingest_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(wsi_ingest_tdrp_struct));
  params->struct_size = sizeof(wsi_ingest_tdrp_struct);

  /* Parameter 'debug_level' */
  /* ctype is 'wsi_ingest_debug_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("debug_level");
  tt->descr = tdrpStrDup("Debug option");
  tt->help = tdrpStrDup("If set, debug messages will be printed appropriately");
  tt->val_offset = (char *) &(pp.debug_level) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("debug_t");
  tt->enum_def.nfields = 4;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
    tt->enum_def.fields[0].val = DEBUG_OFF;
    tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_WARNINGS");
    tt->enum_def.fields[1].val = DEBUG_WARNINGS;
    tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_NORM");
    tt->enum_def.fields[2].val = DEBUG_NORM;
    tt->enum_def.fields[3].name = tdrpStrDup("DEBUG_EXTRA");
    tt->enum_def.fields[3].val = DEBUG_EXTRA;
  tt->single_val.e = DEBUG_OFF;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("Malloc debug level");
  tt->help = tdrpStrDup("0 - none, 1 - corruption checking, 2 - records all malloc blocks and checks, 3 - printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("instance");
  tt->descr = tdrpStrDup("Process instance");
  tt->help = tdrpStrDup("Used for registration with procmap.");
  tt->val_offset = (char *) &(pp.instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'wsi_ip_address' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("wsi_ip_address");
  tt->descr = tdrpStrDup("IP addres of WSI data feed");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.wsi_ip_address) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("129.55.56.8");
  tt++;
  
  /* Parameter 'wsi_port' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("wsi_port");
  tt->descr = tdrpStrDup("port number for WSI data feed");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.wsi_port) - (char *) &pp;
  tt->single_val.l = 8000;
  tt++;
  
  /* Parameter 'output_data' */
  /* ctype is 'wsi_ingest_output_data_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("output_data");
  tt->descr = tdrpStrDup("array of WSI file types to output to files");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.output_data.val) - (char *) &pp;
  tt->len_offset = (char *) &(pp.output_data.len) - (char *) &pp;
  tt->array_offset = (char *) &(pp._output_data) - (char *) &pp;
  tt->array_n_offset = (char *) &(pp.output_data_n) - (char *) &pp;
  tt->is_array = TRUE;
  tt->array_len_fixed = FALSE;
  tt->array_elem_size = sizeof(wsi_ingest_output_data_t);
  tt->array_n = 1;
  tt->struct_def.name = tdrpStrDup("output_data_t");
  tt->struct_def.nfields = 4;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("string");
    tt->struct_def.fields[0].fname = tdrpStrDup("product_type");
    tt->struct_def.fields[0].ptype = STRING_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.output_data.val->product_type) - (char *) pp.output_data.val;
    tt->struct_def.fields[1].ftype = tdrpStrDup("string");
    tt->struct_def.fields[1].fname = tdrpStrDup("product_coverage");
    tt->struct_def.fields[1].ptype = STRING_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.output_data.val->product_coverage) - (char *) pp.output_data.val;
    tt->struct_def.fields[2].ftype = tdrpStrDup("string");
    tt->struct_def.fields[2].fname = tdrpStrDup("output_dir");
    tt->struct_def.fields[2].ptype = STRING_TYPE;
    tt->struct_def.fields[2].rel_offset = 
      (char *) &(pp.output_data.val->output_dir) - (char *) pp.output_data.val;
    tt->struct_def.fields[3].ftype = tdrpStrDup("string");
    tt->struct_def.fields[3].fname = tdrpStrDup("output_ext");
    tt->struct_def.fields[3].ptype = STRING_TYPE;
    tt->struct_def.fields[3].rel_offset = 
      (char *) &(pp.output_data.val->output_ext) - (char *) pp.output_data.val;
  tt->n_struct_vals = 4;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].s = tdrpStrDup("not_set");
    tt->struct_vals[1].s = tdrpStrDup("not_set");
    tt->struct_vals[2].s = tdrpStrDup("not_set");
    tt->struct_vals[3].s = tdrpStrDup("not_set");
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
