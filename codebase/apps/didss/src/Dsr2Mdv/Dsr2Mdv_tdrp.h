/*******************************************
 * Dsr2Mdv_tdrp.h
 *
 * TDRP header file for 'Dsr2Mdv' module.
 *
 * Code for program Dsr2Mdv
 *
 * This header file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#ifndef _Dsr2Mdv_tdrp_h
#define _Dsr2Mdv_tdrp_h

#ifdef __cplusplus
extern "C" {
#endif

#include <tdrp/tdrp.h>

/*
 * typedefs
 */

typedef enum {
  DEBUG_OFF = 0,
  DEBUG_WARNINGS = 1,
  DEBUG_NORM = 2,
  DEBUG_VERBOSE = 3
} Dsr2Mdv_debug_t;

typedef enum {
  END_OF_VOL_FLAG = 0,
  LAST_TILT_IN_VOL = 1
} Dsr2Mdv_end_of_vol_decision_t;

typedef struct {
  double latitude;
  double longitude;
  double altitude;
} Dsr2Mdv_radar_location_t;

typedef struct {
  int scan_type;
  char* lookup_table_path;
  char* clutter_table_path;
} Dsr2Mdv_lookups_t;

/*
 * typedef for main struct - Dsr2Mdv_tdrp_struct
 */

typedef struct {

  size_t struct_size;

  /***** debug *****/

  Dsr2Mdv_debug_t debug;

  /***** malloc_debug_level *****/

  long malloc_debug_level;

  /***** instance *****/

  char* instance;

  /***** input_fmq_url *****/

  char* input_fmq_url;

  /***** seek_to_end_of_input *****/

  tdrp_bool_t seek_to_end_of_input;

  /***** output_mdv_dir *****/

  char* output_mdv_dir;

  /***** radar_info *****/

  char* radar_info;

  /***** override_radar_location *****/

  tdrp_bool_t override_radar_location;

  /***** radar_location *****/

  Dsr2Mdv_radar_location_t radar_location;

  /***** remove_clutter *****/

  tdrp_bool_t remove_clutter;

  /***** lookups *****/

  struct {
    Dsr2Mdv_lookups_t *val;
    int len;
  } lookups;
  Dsr2Mdv_lookups_t *_lookups;
  int lookups_n;

  /***** specify_output_fields *****/

  tdrp_bool_t specify_output_fields;

  /***** output_field_names *****/

  struct {
    char* *val;
    int len;
  } output_field_names;
  char* *_output_field_names;
  int output_field_names_n;

  /***** check_sn *****/

  tdrp_bool_t check_sn;

  /***** sn_threshold *****/

  double sn_threshold;

  /***** sn_min_valid_run *****/

  long sn_min_valid_run;

  /***** noise_dbz_at_100km *****/

  double noise_dbz_at_100km;

  /***** create_time_field *****/

  tdrp_bool_t create_time_field;

  /***** input_time_field_name *****/

  char* input_time_field_name;

  /***** auto_mid_time *****/

  tdrp_bool_t auto_mid_time;

  /***** age_at_end_of_volume *****/

  long age_at_end_of_volume;

  /***** max_vol_duration *****/

  long max_vol_duration;

  /***** azimuth_offset *****/

  double azimuth_offset;

  /***** check_missing_beams *****/

  tdrp_bool_t check_missing_beams;

  /***** max_missing_beams *****/

  long max_missing_beams;

  /***** use_repeated_elevations *****/

  tdrp_bool_t use_repeated_elevations;

  /***** min_elevation *****/

  double min_elevation;

  /***** max_elevation *****/

  double max_elevation;

  /***** end_of_vol_decision *****/

  Dsr2Mdv_end_of_vol_decision_t end_of_vol_decision;

  /***** last_tilt_in_vol *****/

  long last_tilt_in_vol;

  /***** write_intermediate_files *****/

  tdrp_bool_t write_intermediate_files;

  /***** intermediate_file_name *****/

  char* intermediate_file_name;

  /***** intermediate_file_frequency *****/

  long intermediate_file_frequency;

} Dsr2Mdv_tdrp_struct;

/*
 * function prototypes
 */

/*************************************************************
 * Dsr2Mdv_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   Dsr2Mdv_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int Dsr2Mdv_tdrp_load_from_args(int argc, char **argv,
                                Dsr2Mdv_tdrp_struct *params,
                                char **override_list,
                                char **params_path_p);

/*************************************************************
 * Dsr2Mdv_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than Dsr2Mdv_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   Dsr2Mdv_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int Dsr2Mdv_tdrp_load(char *param_file_path,
                      Dsr2Mdv_tdrp_struct *params,
                      char **override_list,
                      int expand_env, int debug);

/*************************************************************
 * Dsr2Mdv_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See Dsr2Mdv_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int Dsr2Mdv_tdrp_load_defaults(Dsr2Mdv_tdrp_struct *params,
                               int expand_env);

/*************************************************************
 * Dsr2Mdv_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

extern void Dsr2Mdv_tdrp_sync(void);

/*************************************************************
 * Dsr2Mdv_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

extern void Dsr2Mdv_tdrp_print(FILE *out, tdrp_print_mode_t mode);

/*************************************************************
 * Dsr2Mdv_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

extern void Dsr2Mdv_tdrp_free_all(void);

/*************************************************************
 * Dsr2Mdv_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

extern int Dsr2Mdv_tdrp_check_all_set(FILE *out);

/*************************************************************
 * Dsr2Mdv_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

extern int Dsr2Mdv_tdrp_check_is_set(char *param_name);

/*************************************************************
 * Dsr2Mdv_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

extern int Dsr2Mdv_tdrp_array_realloc(char *param_name,
                              int new_array_n);

/*************************************************************
 * Dsr2Mdv_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

extern int Dsr2Mdv_tdrp_array2D_realloc(char *param_name,
                                 int new_array_n1,
                                 int new_array_n2);

/*************************************************************
 * Dsr2Mdv_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

extern TDRPtable *Dsr2Mdv_tdrp_table(void);

/*************************************************************
 * Dsr2Mdv_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

extern TDRPtable *Dsr2Mdv_tdrp_init(Dsr2Mdv_tdrp_struct *params);

#ifdef __cplusplus
}
#endif

#endif

