/*******************************************
 * bdry2spdb_tdrp.c
 *
 * TDRP C code file 'bdry2spdb' module.
 *
 * Code for program bdry2spdb
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "bdry2spdb_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[13];
static bdry2spdb_tdrp_struct *Params;
static char *Module = "bdry2spdb";

/*************************************************************
 * bdry2spdb_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   bdry2spdb_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int bdry2spdb_tdrp_load_from_args(int argc, char **argv,
                         bdry2spdb_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  bdry2spdb_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * bdry2spdb_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than bdry2spdb_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   bdry2spdb_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int bdry2spdb_tdrp_load(char *param_file_path,
               bdry2spdb_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  bdry2spdb_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * bdry2spdb_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See bdry2spdb_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int bdry2spdb_tdrp_load_defaults(bdry2spdb_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  bdry2spdb_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * bdry2spdb_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void bdry2spdb_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * bdry2spdb_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void bdry2spdb_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * bdry2spdb_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int bdry2spdb_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * bdry2spdb_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int bdry2spdb_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * bdry2spdb_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void bdry2spdb_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * bdry2spdb_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int bdry2spdb_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * bdry2spdb_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int bdry2spdb_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * bdry2spdb_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *bdry2spdb_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * bdry2spdb_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *bdry2spdb_tdrp_init(bdry2spdb_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  bdry2spdb_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(bdry2spdb_tdrp_struct));
  params->struct_size = sizeof(bdry2spdb_tdrp_struct);

  /* Parameter 'Comment 0' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = COMMENT_TYPE;
  tt->param_name = tdrpStrDup("Comment 0");
  tt->comment_hdr = tdrpStrDup("Reads boundary (e.g. gust front) data in ASCII files and converts to SPDB.");
  tt->comment_text = tdrpStrDup("");
  tt++;
  
  /* Parameter 'debug' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("debug flag");
  tt->help = tdrpStrDup("Debug flag.");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("malloc debug level");
  tt->help = tdrpStrDup("0 = none; 1 = corruption checking, 2 = records all malloc blocks and checks; 3 = printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("instance");
  tt->descr = tdrpStrDup("procmap instance");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'mode' */
  /* ctype is 'bdry2spdb_mode_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("mode");
  tt->descr = tdrpStrDup("operational mode");
  tt->help = tdrpStrDup("In ARCHIVE mode, exits after processing all files in the input directory; in REALTIME mode, keeps looking for new files in the input directory and processes them as they arrive.");
  tt->val_offset = (char *) &(pp.mode) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("mode_t");
  tt->enum_def.nfields = 2;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("REALTIME");
    tt->enum_def.fields[0].val = REALTIME;
    tt->enum_def.fields[1].name = tdrpStrDup("ARCHIVE");
    tt->enum_def.fields[1].val = ARCHIVE;
  tt->single_val.e = REALTIME;
  tt++;
  
  /* Parameter 'sleep_seconds' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("sleep_seconds");
  tt->descr = tdrpStrDup("sleep seconds");
  tt->help = tdrpStrDup("Number of seconds to sleep between looking for new input files.");
  tt->val_offset = (char *) &(pp.sleep_seconds) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.l = 1;
  tt->single_val.l = 2;
  tt++;
  
  /* Parameter 'processing_delay' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("processing_delay");
  tt->descr = tdrpStrDup("processing delay in seconds");
  tt->help = tdrpStrDup("The number of seconds to wait before processing an input file.  This delay allows activity on the file to finish before we start reading it.");
  tt->val_offset = (char *) &(pp.processing_delay) - (char *) &pp;
  tt->single_val.l = 1;
  tt++;
  
  /* Parameter 'input_dir' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("input_dir");
  tt->descr = tdrpStrDup("input directory");
  tt->help = tdrpStrDup("Path for directory where Kavouras is putting the lightning files.");
  tt->val_offset = (char *) &(pp.input_dir) - (char *) &pp;
  tt->single_val.s = tdrpStrDup(".");
  tt++;
  
  /* Parameter 'input_substring' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("input_substring");
  tt->descr = tdrpStrDup("input file substring");
  tt->help = tdrpStrDup("Substring which is a part of every input file to be processed.");
  tt->val_offset = (char *) &(pp.input_substring) - (char *) &pp;
  tt->single_val.s = tdrpStrDup(".ltg");
  tt++;
  
  /* Parameter 'expire_secs' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("expire_secs");
  tt->descr = tdrpStrDup("expire seconds");
  tt->help = tdrpStrDup("If >= 0, adds the given number of seconds to the product data time to determine the database expire time; if < 0, uses the expire time in the product as the expire time in the database.");
  tt->val_offset = (char *) &(pp.expire_secs) - (char *) &pp;
  tt->single_val.l = 360;
  tt++;
  
  /* Parameter 'process_old_data' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("process_old_data");
  tt->descr = tdrpStrDup("process old data flag");
  tt->help = tdrpStrDup("If TRUE, all existing files in the input directory will be processed; if FALSE, only files received after starting this process will be used.");
  tt->val_offset = (char *) &(pp.process_old_data) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'spdb_destinations' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("spdb_destinations");
  tt->descr = tdrpStrDup("SPDB destinations");
  tt->help = tdrpStrDup("An array of destinations for the SPDB lightning data.  Note that the first element in this array MUST be the local disk version of the database if check_old_data is set to TRUE.");
  tt->val_offset = (char *) &(pp.spdb_destinations.val) - (char *) &pp;
  tt->len_offset = (char *) &(pp.spdb_destinations.len) - (char *) &pp;
  tt->array_offset = (char *) &(pp._spdb_destinations) - (char *) &pp;
  tt->array_n_offset = (char *) &(pp.spdb_destinations_n) - (char *) &pp;
  tt->is_array = TRUE;
  tt->array_len_fixed = FALSE;
  tt->array_elem_size = sizeof(char*);
  tt->array_n = 1;
  tt->array_vals = (tdrpVal_t *)
      tdrpMalloc(tt->array_n * sizeof(tdrpVal_t));
    tt->array_vals[0].s = tdrpStrDup("not_set");
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
