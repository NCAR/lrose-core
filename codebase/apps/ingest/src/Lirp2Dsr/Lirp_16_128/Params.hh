////////////////////////////////////////////
// Params.hh
//
// TDRP header file for 'Params' class.
//
// Code for program Lirp_16_128
//
// This header file has been automatically
// generated by TDRP, do not modify.
//
/////////////////////////////////////////////

/**
 *
 * @file Params.hh
 *
 * This class is automatically generated by the Table
 * Driven Runtime Parameters (TDRP) system
 *
 * @author automatically generated
 *
 */

#ifndef Params_hh
#define Params_hh

using namespace std;

#include <tdrp/tdrp.h>
#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <climits>
#include <cfloat>

// Class definition

class Params {

public:

  // enum typedefs

  typedef enum {
    DEBUG_OFF = 0,
    DEBUG_NORM = 1,
    DEBUG_VERBOSE = 2,
    DEBUG_EXTRA_VERBOSE = 3
  } debug_t;

  typedef enum {
    ARCHIVE = 0,
    REALTIME = 1,
    SIMULATE = 2
  } mode_t;

  typedef enum {
    HORIZONTAL = 0,
    VERTICAL = 1,
    RIGHT_CIRCULAR = 2,
    ELLIPTICAL = 3,
    LEFT_CIRCULAR = 4,
    DUAL = 5
  } polar_t;

  typedef enum {
    ALG_PP = 0,
    ALG_FFT = 1
  } algorithm_t;

  typedef enum {
    WINDOW_HANNING = 0,
    WINDOW_BLACKMAN = 1,
    WINDOW_NONE = 2
  } fft_window_t;

  typedef enum {
    NO_WINDOW = 0,
    HANNING = 1,
    BLACKMAN = 2
  } sz_window_t;

  // struct typedefs

  typedef struct {
    int radar_id;
    char* radar_name;
    char* site_name;
    polar_t polarization;
    double altitude;
    double latitude;
    double longitude;
    double horiz_beam_width;
    double vert_beam_width;
    double wavelength;
    double radar_constant;
    double xmit_peak_pwr;
    double receiver_mds;
    double receiver_gain;
    double antenna_gain;
    double system_gain;
    double noise_value;
    double min_dbz_at_1km;
  } radar_params_t;

  typedef struct {
    double lower_prf;
    double upper_prf;
    double pulse_width;
    double start_range;
    double gate_spacing;
    algorithm_t algorithm;
    fft_window_t window;
    tdrp_bool_t apply_sz;
    tdrp_bool_t dual_pol;
    int n_samples;
  } moments_params_t;

  typedef struct {
    double min_el;
    double max_el;
    double min_az;
    double max_az;
    double min_range;
    double max_range;
  } selected_region_t;

  typedef struct {
    double value;
    double interest;
  } interest_map_point_t;

  ///////////////////////////
  // Member functions
  //

  ////////////////////////////////////////////
  // Default constructor
  //

  Params ();

  ////////////////////////////////////////////
  // Copy constructor
  //

  Params (const Params&);

  ////////////////////////////////////////////
  // Destructor
  //

  ~Params ();

  ////////////////////////////////////////////
  // Assignment
  //

  void operator=(const Params&);

  ////////////////////////////////////////////
  // loadFromArgs()
  //
  // Loads up TDRP using the command line args.
  //
  // Check usage() for command line actions associated with
  // this function.
  //
  //   argc, argv: command line args
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   char **params_path_p:
  //     If this is non-NULL, it is set to point to the path
  //     of the params file used.
  //
  //   bool defer_exit: normally, if the command args contain a 
  //      print or check request, this function will call exit().
  //      If defer_exit is set, such an exit is deferred and the
  //      private member _exitDeferred is set.
  //      Use exidDeferred() to test this flag.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int loadFromArgs(int argc, char **argv,
                   char **override_list,
                   char **params_path_p,
                   bool defer_exit = false);

  bool exitDeferred() { return (_exitDeferred); }

  ////////////////////////////////////////////
  // loadApplyArgs()
  //
  // Loads up TDRP using the params path passed in, and applies
  // the command line args for printing and checking.
  //
  // Check usage() for command line actions associated with
  // this function.
  //
  //   const char *param_file_path: the parameter file to be read in
  //
  //   argc, argv: command line args
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   bool defer_exit: normally, if the command args contain a 
  //      print or check request, this function will call exit().
  //      If defer_exit is set, such an exit is deferred and the
  //      private member _exitDeferred is set.
  //      Use exidDeferred() to test this flag.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int loadApplyArgs(const char *params_path,
                    int argc, char **argv,
                    char **override_list,
                    bool defer_exit = false);

  ////////////////////////////////////////////
  // isArgValid()
  // 
  // Check if a command line arg is a valid TDRP arg.
  //

  static bool isArgValid(const char *arg);

  ////////////////////////////////////////////
  // load()
  //
  // Loads up TDRP for a given class.
  //
  // This version of load gives the programmer the option to load
  // up more than one class for a single application. It is a
  // lower-level routine than loadFromArgs, and hence more
  // flexible, but the programmer must do more work.
  //
  //   const char *param_file_path: the parameter file to be read in.
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   expand_env: flag to control environment variable
  //               expansion during tokenization.
  //               If TRUE, environment expansion is set on.
  //               If FALSE, environment expansion is set off.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int load(const char *param_file_path,
           char **override_list,
           int expand_env, int debug);

  ////////////////////////////////////////////
  // loadFromBuf()
  //
  // Loads up TDRP for a given class.
  //
  // This version of load gives the programmer the option to
  // load up more than one module for a single application,
  // using buffers which have been read from a specified source.
  //
  //   const char *param_source_str: a string which describes the
  //     source of the parameter information. It is used for
  //     error reporting only.
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   const char *inbuf: the input buffer
  //
  //   int inlen: length of the input buffer
  //
  //   int start_line_num: the line number in the source which
  //     corresponds to the start of the buffer.
  //
  //   expand_env: flag to control environment variable
  //               expansion during tokenization.
  //               If TRUE, environment expansion is set on.
  //               If FALSE, environment expansion is set off.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int loadFromBuf(const char *param_source_str,
                  char **override_list,
                  const char *inbuf, int inlen,
                  int start_line_num,
                  int expand_env, int debug);

  ////////////////////////////////////////////
  // loadDefaults()
  //
  // Loads up default params for a given class.
  //
  // See load() for more detailed info.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int loadDefaults(int expand_env);

  ////////////////////////////////////////////
  // sync()
  //
  // Syncs the user struct data back into the parameter table,
  // in preparation for printing.
  //
  // This function alters the table in a consistent manner.
  // Therefore it can be regarded as const.
  //

  void sync() const;

  ////////////////////////////////////////////
  // print()
  // 
  // Print params file
  //
  // The modes supported are:
  //
  //   PRINT_SHORT:   main comments only, no help or descriptions
  //                  structs and arrays on a single line
  //   PRINT_NORM:    short + descriptions and help
  //   PRINT_LONG:    norm  + arrays and structs expanded
  //   PRINT_VERBOSE: long  + private params included
  //

  void print(FILE *out, tdrp_print_mode_t mode = PRINT_NORM);

  ////////////////////////////////////////////
  // checkAllSet()
  //
  // Return TRUE if all set, FALSE if not.
  //
  // If out is non-NULL, prints out warning messages for those
  // parameters which are not set.
  //

  int checkAllSet(FILE *out);

  //////////////////////////////////////////////////////////////
  // checkIsSet()
  //
  // Return TRUE if parameter is set, FALSE if not.
  //
  //

  int checkIsSet(const char *param_name);

  ////////////////////////////////////////////
  // arrayRealloc()
  //
  // Realloc 1D array.
  //
  // If size is increased, the values from the last array 
  // entry is copied into the new space.
  //
  // Returns 0 on success, -1 on error.
  //

  int arrayRealloc(const char *param_name,
                   int new_array_n);

  ////////////////////////////////////////////
  // array2DRealloc()
  //
  // Realloc 2D array.
  //
  // If size is increased, the values from the last array 
  // entry is copied into the new space.
  //
  // Returns 0 on success, -1 on error.
  //

  int array2DRealloc(const char *param_name,
                     int new_array_n1,
                     int new_array_n2);

  ////////////////////////////////////////////
  // freeAll()
  //
  // Frees up all TDRP dynamic memory.
  //

  void freeAll(void);

  ////////////////////////////////////////////
  // usage()
  //
  // Prints out usage message for TDRP args as passed
  // in to loadFromArgs().
  //

  static void usage(ostream &out);

  ///////////////////////////
  // Data Members
  //

  char _start_; // start of data region
                // needed for zeroing out data
                // and computing offsets

  debug_t debug;

  char* instance;

  tdrp_bool_t print_summary;

  tdrp_bool_t compute_noise;

  mode_t mode;

  char* input_dir;

  tdrp_bool_t use_ldata_info_file;

  int max_realtime_valid_age;

  radar_params_t radar;

  int scan_type_id;

  char* scan_type_name;

  double atmos_attenuation;

  double ifd_sampling_freq;

  double dual_pol_h_v_phase_diff;

  tdrp_bool_t index_beams_in_azimuth;

  double azimuth_resolution;

  double H2V_phase_differential;

  moments_params_t *_moments_params;
  int moments_params_n;

  tdrp_bool_t use_c_for_sz;

  tdrp_bool_t negate_phase_codes;

  double signal_to_noise_ratio_threshold;

  double sz_strong_to_weak_power_ratio_threshold;

  double sz_out_of_trip_power_ratio_threshold;

  int sz_out_of_trip_power_n_replicas;

  sz_window_t sz_window;

  selected_region_t selected_region;

  tdrp_bool_t do_selected_print;

  char* spectra_dir;

  tdrp_bool_t write_individual_spectra_files;

  tdrp_bool_t write_combined_spectra_file;

  double min_snr_for_combined_spectra_file;

  tdrp_bool_t apply_rec;

  int rec_kernel_range_len;

  int rec_kernel_azimuth_width;

  double rec_spin_dbz_threshold;

  tdrp_bool_t use_dbz_narrow_for_tdbz_and_spin;

  interest_map_point_t *_dbz_texture_interest_map;
  int dbz_texture_interest_map_n;

  double dbz_texture_interest_weight;

  interest_map_point_t *_sqrt_texture_interest_map;
  int sqrt_texture_interest_map_n;

  double sqrt_texture_interest_weight;

  interest_map_point_t *_dbz_spin_interest_map;
  int dbz_spin_interest_map_n;

  double dbz_spin_interest_weight;

  interest_map_point_t *_wt_spin_interest_map;
  int wt_spin_interest_map_n;

  double wt_spin_interest_weight;

  interest_map_point_t *_vel_interest_map;
  int vel_interest_map_n;

  double vel_interest_weight;

  interest_map_point_t *_width_interest_map;
  int width_interest_map_n;

  double width_interest_weight;

  interest_map_point_t *_vel_sdev_interest_map;
  int vel_sdev_interest_map_n;

  double vel_sdev_interest_weight;

  interest_map_point_t *_zdr_sdev_interest_map;
  int zdr_sdev_interest_map_n;

  double zdr_sdev_interest_weight;

  interest_map_point_t *_rhohv_interest_map;
  int rhohv_interest_map_n;

  double rhohv_interest_weight;

  interest_map_point_t *_rhohv_sdev_interest_map;
  int rhohv_sdev_interest_map_n;

  double rhohv_sdev_interest_weight;

  interest_map_point_t *_clut_ratio_narrow_interest_map;
  int clut_ratio_narrow_interest_map_n;

  double clut_ratio_narrow_interest_weight;

  interest_map_point_t *_clut_ratio_wide_interest_map;
  int clut_ratio_wide_interest_map_n;

  double clut_ratio_wide_interest_weight;

  interest_map_point_t *_clut_wx_peak_ratio_interest_map;
  int clut_wx_peak_ratio_interest_map_n;

  double clut_wx_peak_ratio_interest_weight;

  interest_map_point_t *_clut_wx_peak_sep_interest_map;
  int clut_wx_peak_sep_interest_map_n;

  double clut_wx_peak_sep_interest_weight;

  interest_map_point_t *_clut_wx_peak_sep_sdev_interest_map;
  int clut_wx_peak_sep_sdev_interest_map_n;

  double clut_wx_peak_sep_sdev_interest_weight;

  tdrp_bool_t filter_clutter_using_rec;

  double min_ratio_narrow_clutter;

  double rec_threshold_for_clutter;

  tdrp_bool_t output_snr;

  tdrp_bool_t output_dbz_total;

  tdrp_bool_t output_dbz;

  tdrp_bool_t output_velocity;

  tdrp_bool_t output_width;

  tdrp_bool_t output_filtered;

  tdrp_bool_t output_infilled;

  tdrp_bool_t output_flags;

  tdrp_bool_t output_trip_flag;

  tdrp_bool_t output_infill_flags;

  tdrp_bool_t output_censoring_debug;

  tdrp_bool_t output_infill_debug;

  tdrp_bool_t output_dual_pol_fields;

  tdrp_bool_t output_rec;

  tdrp_bool_t output_rec_debug;

  tdrp_bool_t output_noise_fields;

  tdrp_bool_t output_clutter_fields;

  char* output_fmq_url;

  int output_fmq_size;

  int output_fmq_nslots;

  tdrp_bool_t output_fmq_compress;

  tdrp_bool_t write_blocking;

  int beam_wait_msecs;

  tdrp_bool_t set_end_of_vol_from_elev_angle;

  tdrp_bool_t vol_starts_at_bottom;

  double elev_change_for_end_of_vol;

  int min_beams_per_vol;

  tdrp_bool_t set_end_of_vol_on_prf_change;

  char _end_; // end of data region
              // needed for zeroing out data

private:

  void _init();

  mutable TDRPtable _table[109];

  const char *_className;

  bool _exitDeferred;

};

#endif

