/*******************************************
 * tstorms_spdb2symprod_tdrp.c
 *
 * TDRP C code file 'tstorms_spdb2symprod' module.
 *
 * Code for program tstorms_spdb2symprod
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "tstorms_spdb2symprod_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[41];
static tstorms_spdb2symprod_tdrp_struct *Params;
static char *Module = "tstorms_spdb2symprod";

/*************************************************************
 * tstorms_spdb2symprod_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   tstorms_spdb2symprod_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int tstorms_spdb2symprod_tdrp_load_from_args(int argc, char **argv,
                         tstorms_spdb2symprod_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  tstorms_spdb2symprod_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than tstorms_spdb2symprod_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   tstorms_spdb2symprod_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int tstorms_spdb2symprod_tdrp_load(char *param_file_path,
               tstorms_spdb2symprod_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  tstorms_spdb2symprod_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See tstorms_spdb2symprod_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int tstorms_spdb2symprod_tdrp_load_defaults(tstorms_spdb2symprod_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  tstorms_spdb2symprod_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void tstorms_spdb2symprod_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void tstorms_spdb2symprod_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int tstorms_spdb2symprod_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int tstorms_spdb2symprod_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void tstorms_spdb2symprod_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int tstorms_spdb2symprod_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int tstorms_spdb2symprod_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *tstorms_spdb2symprod_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * tstorms_spdb2symprod_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *tstorms_spdb2symprod_tdrp_init(tstorms_spdb2symprod_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  tstorms_spdb2symprod_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(tstorms_spdb2symprod_tdrp_struct));
  params->struct_size = sizeof(tstorms_spdb2symprod_tdrp_struct);

  /* Parameter 'debug' */
  /* ctype is 'tstorms_spdb2symprod_debug_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("debug level");
  tt->help = tdrpStrDup("Debug level: DEBUG_OFF, DEBUG_ERRORS, DEBUG_MSGS, DEBUG_ROUTINES, DEBUG_ALL.");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("debug_t");
  tt->enum_def.nfields = 5;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
    tt->enum_def.fields[0].val = DEBUG_OFF;
    tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_ERRORS");
    tt->enum_def.fields[1].val = DEBUG_ERRORS;
    tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_MSGS");
    tt->enum_def.fields[2].val = DEBUG_MSGS;
    tt->enum_def.fields[3].name = tdrpStrDup("DEBUG_ROUTINES");
    tt->enum_def.fields[3].val = DEBUG_ROUTINES;
    tt->enum_def.fields[4].name = tdrpStrDup("DEBUG_ALL");
    tt->enum_def.fields[4].val = DEBUG_ALL;
  tt->single_val.e = DEBUG_OFF;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("malloc debug level");
  tt->help = tdrpStrDup("0 = none, 1 = corruption checking, 2 = record all malloc blocks and checks, 3 = printout of all mallocs, etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'database_dir' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("database_dir");
  tt->descr = tdrpStrDup("database directory");
  tt->help = tdrpStrDup("Directory containing SPDB database.");
  tt->val_offset = (char *) &(pp.database_dir) - (char *) &pp;
  tt->single_val.s = tdrpStrDup(".");
  tt++;
  
  /* Parameter 'product_label' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("product_label");
  tt->descr = tdrpStrDup("product label");
  tt->help = tdrpStrDup("The product label used in the SPDB database to uniquely identify this product.");
  tt->val_offset = (char *) &(pp.product_label) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("TITAN_STORMS");
  tt++;
  
  /* Parameter 'product_id' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("product_id");
  tt->descr = tdrpStrDup("product id");
  tt->help = tdrpStrDup("Number which uniquely identifies this product in the SPDB database.");
  tt->val_offset = (char *) &(pp.product_id) - (char *) &pp;
  tt->single_val.l = 20020;
  tt++;
  
  /* Parameter 'port' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("port");
  tt->descr = tdrpStrDup("port");
  tt->help = tdrpStrDup("Server port number.");
  tt->val_offset = (char *) &(pp.port) - (char *) &pp;
  tt->single_val.l = 62020;
  tt++;
  
  /* Parameter 'servmap_type' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("servmap_type");
  tt->descr = tdrpStrDup("servmap type");
  tt->help = tdrpStrDup("Server mapper type for this server.");
  tt->val_offset = (char *) &(pp.servmap_type) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Spdb");
  tt++;
  
  /* Parameter 'servmap_subtype' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("servmap_subtype");
  tt->descr = tdrpStrDup("servmap subtype");
  tt->help = tdrpStrDup("Server mapper subtype for this server.  The type is always Spdb.");
  tt->val_offset = (char *) &(pp.servmap_subtype) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Tstorms");
  tt++;
  
  /* Parameter 'servmap_instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("servmap_instance");
  tt->descr = tdrpStrDup("servmap instance");
  tt->help = tdrpStrDup("Server mapper instance for this server.  The type is always Spdb.");
  tt->val_offset = (char *) &(pp.servmap_instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'realtime_avail' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("realtime_avail");
  tt->descr = tdrpStrDup("realtime available flag");
  tt->help = tdrpStrDup("Flag indicating if realtime data is available from this server.");
  tt->val_offset = (char *) &(pp.realtime_avail) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'valid_forecasts_only' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("valid_forecasts_only");
  tt->descr = tdrpStrDup("Valid forecasts only flag");
  tt->help = tdrpStrDup("If set, only valid forecasts will be sent. If not, all forecasts will be sent.");
  tt->val_offset = (char *) &(pp.valid_forecasts_only) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'plot_current' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("plot_current");
  tt->descr = tdrpStrDup("Plot current shape flag");
  tt->help = tdrpStrDup("If set, the current storm shape will be sent.");
  tt->val_offset = (char *) &(pp.plot_current) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'plot_forecast' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("plot_forecast");
  tt->descr = tdrpStrDup("Plot forecast shape flag");
  tt->help = tdrpStrDup("If set, the forecast storm shape will be sent.");
  tt->val_offset = (char *) &(pp.plot_forecast) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'forecast_lead_time' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("forecast_lead_time");
  tt->descr = tdrpStrDup("Forecast lead time (secs).");
  tt->help = tdrpStrDup("Lead time for forecast shapes and positions.");
  tt->val_offset = (char *) &(pp.forecast_lead_time) - (char *) &pp;
  tt->single_val.l = 1800;
  tt++;
  
  /* Parameter 'forecast_dashed' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("forecast_dashed");
  tt->descr = tdrpStrDup("Forecast line dashed flag");
  tt->help = tdrpStrDup("If set, the forecast will be rendered using a dashed line rather than a solid line.");
  tt->val_offset = (char *) &(pp.forecast_dashed) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'storm_shape' */
  /* ctype is 'tstorms_spdb2symprod_storm_shape' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("storm_shape");
  tt->descr = tdrpStrDup("Shape type");
  tt->help = tdrpStrDup("Determines the way in which the storm is represented.");
  tt->val_offset = (char *) &(pp.storm_shape) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("storm_shape");
  tt->enum_def.nfields = 2;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("ELLIPSE_SHAPE");
    tt->enum_def.fields[0].val = ELLIPSE_SHAPE;
    tt->enum_def.fields[1].name = tdrpStrDup("POLYGON_SHAPE");
    tt->enum_def.fields[1].val = POLYGON_SHAPE;
  tt->single_val.e = POLYGON_SHAPE;
  tt++;
  
  /* Parameter 'plot_trend' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("plot_trend");
  tt->descr = tdrpStrDup("Plot trend");
  tt->help = tdrpStrDup("If set, the storm size and intensity trend will be included as text: '+' or '-'.");
  tt->val_offset = (char *) &(pp.plot_trend) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'plot_speed' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("plot_speed");
  tt->descr = tdrpStrDup("Plot speed");
  tt->help = tdrpStrDup("If set, speed is placed at the head of the vector.");
  tt->val_offset = (char *) &(pp.plot_speed) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'speed_units' */
  /* ctype is 'tstorms_spdb2symprod_speed_units_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("speed_units");
  tt->descr = tdrpStrDup("Units for plotting speed.");
  tt->help = tdrpStrDup("The speed is computed in knk. The speed will be converted and plotted in these units.");
  tt->val_offset = (char *) &(pp.speed_units) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("speed_units_t");
  tt->enum_def.nfields = 3;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("SPEED_KMH");
    tt->enum_def.fields[0].val = SPEED_KMH;
    tt->enum_def.fields[1].name = tdrpStrDup("SPEED_KNOTS");
    tt->enum_def.fields[1].val = SPEED_KNOTS;
    tt->enum_def.fields[2].name = tdrpStrDup("SPEED_MPH");
    tt->enum_def.fields[2].val = SPEED_MPH;
  tt->single_val.e = SPEED_KMH;
  tt++;
  
  /* Parameter 'speed_round' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("speed_round");
  tt->descr = tdrpStrDup("Plot speed rounded to nearest 5.");
  tt->help = tdrpStrDup("Default is not rounded.");
  tt->val_offset = (char *) &(pp.speed_round) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'plot_top' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("plot_top");
  tt->descr = tdrpStrDup("Plot top");
  tt->help = tdrpStrDup("If set, top value is placed at the head of the vector.");
  tt->val_offset = (char *) &(pp.plot_top) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'top_km' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("top_km");
  tt->descr = tdrpStrDup("Plot top value in kilometers");
  tt->help = tdrpStrDup("If FALSE, top value is plotted in 100s of feet.");
  tt->val_offset = (char *) &(pp.top_km) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'hull_smooth' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("hull_smooth");
  tt->descr = tdrpStrDup("Smooth the storm polygon");
  tt->help = tdrpStrDup("If TRUE and storm_shape is POLYGON_SHAPE, the storm polygon will be smoothed within the boundaries defined by inner_bnd_multiplier and outer_bnd_multiplier.");
  tt->val_offset = (char *) &(pp.hull_smooth) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'inner_bnd_multiplier' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("inner_bnd_multiplier");
  tt->descr = tdrpStrDup("Inner boundary multiplier for storm smoothing");
  tt->help = tdrpStrDup("Multipliers less than 1.0 produce an inner boundary smaller than the original storm. Multipliers greater than 1.0 produce an inner boundary larger than the original storm. inner_bnd_multiplier must be <= outer_bnd_multiplier");
  tt->val_offset = (char *) &(pp.inner_bnd_multiplier) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 1;
  tt++;
  
  /* Parameter 'outer_bnd_multiplier' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("outer_bnd_multiplier");
  tt->descr = tdrpStrDup("Outer boundary multiplier for storm smoothing");
  tt->help = tdrpStrDup("Multipliers less than 1.0 produce an outer boundary smaller than the original storm. Multipliers greater than 1.0 produce an outer boundary larger than the original storm. outer_bnd_multiplier must be >= inner_bnd_multiplier");
  tt->val_offset = (char *) &(pp.outer_bnd_multiplier) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 1;
  tt++;
  
  /* Parameter 'plot_vectors' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("plot_vectors");
  tt->descr = tdrpStrDup("Plot vector flag");
  tt->help = tdrpStrDup("If set, the forecast vector be sent.");
  tt->val_offset = (char *) &(pp.plot_vectors) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'arrow_head_len' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("arrow_head_len");
  tt->descr = tdrpStrDup("Length of arrow head (km).");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.arrow_head_len) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 2;
  tt++;
  
  /* Parameter 'arrow_head_half_angle' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("arrow_head_half_angle");
  tt->descr = tdrpStrDup("Half angle of arrow head (deg).");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.arrow_head_half_angle) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = 0;
  tt->max_val.d = 90;
  tt->single_val.d = 20;
  tt++;
  
  /* Parameter 'fixed_length_arrows' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("fixed_length_arrows");
  tt->descr = tdrpStrDup("Fixed length arrows flag");
  tt->help = tdrpStrDup("If flag set, direction arrows are of fixed lenght. If not set, arrows are scaled to length for time forecast_lead_time.");
  tt->val_offset = (char *) &(pp.fixed_length_arrows) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'arrow_shaft_length' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("arrow_shaft_length");
  tt->descr = tdrpStrDup("Shaft length arrow head (km).");
  tt->help = tdrpStrDup("This is used if 'fixed_length_arrows' is TRUE.");
  tt->val_offset = (char *) &(pp.arrow_shaft_length) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 20;
  tt++;
  
  /* Parameter 'current_color' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("current_color");
  tt->descr = tdrpStrDup("current color");
  tt->help = tdrpStrDup("Name of color suggested for rendering the current storm.");
  tt->val_offset = (char *) &(pp.current_color) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("yellow");
  tt++;
  
  /* Parameter 'forecast_color' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("forecast_color");
  tt->descr = tdrpStrDup("forecast color");
  tt->help = tdrpStrDup("Name of color suggested for rendering the forecast storm.");
  tt->val_offset = (char *) &(pp.forecast_color) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("yellow");
  tt++;
  
  /* Parameter 'vector_color' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("vector_color");
  tt->descr = tdrpStrDup("motion vector color");
  tt->help = tdrpStrDup("Name of color suggested for rendering the motion vector.");
  tt->val_offset = (char *) &(pp.vector_color) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("yellow");
  tt++;
  
  /* Parameter 'text_color' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("text_color");
  tt->descr = tdrpStrDup("text color");
  tt->help = tdrpStrDup("Name of color suggested for rendering the text.");
  tt->val_offset = (char *) &(pp.text_color) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("yellow");
  tt++;
  
  /* Parameter 'text_background_color' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("text_background_color");
  tt->descr = tdrpStrDup("text background color");
  tt->help = tdrpStrDup("Name of color suggested for rendering the background for the text.  Set to blank string (the default) to use the default background color.");
  tt->val_offset = (char *) &(pp.text_background_color) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("");
  tt++;
  
  /* Parameter 'text_font' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("text_font");
  tt->descr = tdrpStrDup("text font name");
  tt->help = tdrpStrDup("The name of the X font to be used when rendering the text.");
  tt->val_offset = (char *) &(pp.text_font) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("8x13");
  tt++;
  
  /* Parameter 'suggested_line_type' */
  /* ctype is 'tstorms_spdb2symprod_line_type_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("suggested_line_type");
  tt->descr = tdrpStrDup("display line type");
  tt->help = tdrpStrDup("Line type suggested for using on the display.");
  tt->val_offset = (char *) &(pp.suggested_line_type) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("line_type_t");
  tt->enum_def.nfields = 3;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("LINETYPE_SOLID");
    tt->enum_def.fields[0].val = LINETYPE_SOLID;
    tt->enum_def.fields[1].name = tdrpStrDup("LINETYPE_DASH");
    tt->enum_def.fields[1].val = LINETYPE_DASH;
    tt->enum_def.fields[2].name = tdrpStrDup("LINETYPE_DOT_DASH");
    tt->enum_def.fields[2].val = LINETYPE_DOT_DASH;
  tt->single_val.e = LINETYPE_SOLID;
  tt++;
  
  /* Parameter 'suggested_line_width' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("suggested_line_width");
  tt->descr = tdrpStrDup("display line width");
  tt->help = tdrpStrDup("Line width suggested for using on the display.");
  tt->val_offset = (char *) &(pp.suggested_line_width) - (char *) &pp;
  tt->single_val.l = 1;
  tt++;
  
  /* Parameter 'suggested_capstyle' */
  /* ctype is 'tstorms_spdb2symprod_capstyle_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("suggested_capstyle");
  tt->descr = tdrpStrDup("display capstyle");
  tt->help = tdrpStrDup("Capstyle suggested for using on the display.");
  tt->val_offset = (char *) &(pp.suggested_capstyle) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("capstyle_t");
  tt->enum_def.nfields = 4;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("CAPSTYLE_BUTT");
    tt->enum_def.fields[0].val = CAPSTYLE_BUTT;
    tt->enum_def.fields[1].name = tdrpStrDup("CAPSTYLE_NOT_LAST");
    tt->enum_def.fields[1].val = CAPSTYLE_NOT_LAST;
    tt->enum_def.fields[2].name = tdrpStrDup("CAPSTYLE_PROJECTING");
    tt->enum_def.fields[2].val = CAPSTYLE_PROJECTING;
    tt->enum_def.fields[3].name = tdrpStrDup("CAPSTYLE_ROUND");
    tt->enum_def.fields[3].val = CAPSTYLE_ROUND;
  tt->single_val.e = CAPSTYLE_BUTT;
  tt++;
  
  /* Parameter 'suggested_joinstyle' */
  /* ctype is 'tstorms_spdb2symprod_joinstyle_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("suggested_joinstyle");
  tt->descr = tdrpStrDup("display joinstyle");
  tt->help = tdrpStrDup("Joinstyle suggested for using on the display.");
  tt->val_offset = (char *) &(pp.suggested_joinstyle) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("joinstyle_t");
  tt->enum_def.nfields = 3;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("JOINSTYLE_BEVEL");
    tt->enum_def.fields[0].val = JOINSTYLE_BEVEL;
    tt->enum_def.fields[1].name = tdrpStrDup("JOINSTYLE_MITER");
    tt->enum_def.fields[1].val = JOINSTYLE_MITER;
    tt->enum_def.fields[2].name = tdrpStrDup("JOINSTYLE_ROUND");
    tt->enum_def.fields[2].val = JOINSTYLE_ROUND;
  tt->single_val.e = JOINSTYLE_BEVEL;
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
