/*******************************************
 * verify_grid_tdrp.c
 *
 * TDRP C code file 'verify_grid' module.
 *
 * Code for program verify_grid
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "verify_grid_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[29];
static verify_grid_tdrp_struct *Params;
static char *Module = "verify_grid";

/*************************************************************
 * verify_grid_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   verify_grid_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int verify_grid_tdrp_load_from_args(int argc, char **argv,
                         verify_grid_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  verify_grid_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * verify_grid_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than verify_grid_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   verify_grid_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int verify_grid_tdrp_load(char *param_file_path,
               verify_grid_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  verify_grid_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * verify_grid_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See verify_grid_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int verify_grid_tdrp_load_defaults(verify_grid_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  verify_grid_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * verify_grid_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void verify_grid_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * verify_grid_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void verify_grid_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * verify_grid_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int verify_grid_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * verify_grid_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int verify_grid_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * verify_grid_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void verify_grid_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * verify_grid_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int verify_grid_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * verify_grid_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int verify_grid_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * verify_grid_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *verify_grid_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * verify_grid_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *verify_grid_tdrp_init(verify_grid_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  verify_grid_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(verify_grid_tdrp_struct));
  params->struct_size = sizeof(verify_grid_tdrp_struct);

  /* Parameter 'debug' */
  /* ctype is 'verify_grid_debug' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("Debug option");
  tt->help = tdrpStrDup("If set, debug messages will be printed appropriately");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("debug");
  tt->enum_def.nfields = 4;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
    tt->enum_def.fields[0].val = DEBUG_OFF;
    tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_WARNINGS");
    tt->enum_def.fields[1].val = DEBUG_WARNINGS;
    tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_NORM");
    tt->enum_def.fields[2].val = DEBUG_NORM;
    tt->enum_def.fields[3].name = tdrpStrDup("DEBUG_EXTRA");
    tt->enum_def.fields[3].val = DEBUG_EXTRA;
  tt->single_val.e = DEBUG_OFF;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("Malloc debug level");
  tt->help = tdrpStrDup("0 - none, 1 - corruption checking, 2 - records all malloc blocks and checks, 3 - printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'mode' */
  /* ctype is 'verify_grid_mode' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("mode");
  tt->descr = tdrpStrDup("Operation mode");
  tt->help = tdrpStrDup("Modes are 1) cont - contingency table analysis, with text output, 2) stats analysis, with text and xgraph output, 3) regression - data pairs for plotting with ascii_to_xgraph, output to stdout");
  tt->val_offset = (char *) &(pp.mode) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("mode");
  tt->enum_def.nfields = 3;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("CONT");
    tt->enum_def.fields[0].val = CONT;
    tt->enum_def.fields[1].name = tdrpStrDup("STATS");
    tt->enum_def.fields[1].val = STATS;
    tt->enum_def.fields[2].name = tdrpStrDup("REGRESSION");
    tt->enum_def.fields[2].val = REGRESSION;
  tt->single_val.e = STATS;
  tt++;
  
  /* Parameter 'truth_data_dir' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("truth_data_dir");
  tt->descr = tdrpStrDup("Truth data directory");
  tt->help = tdrpStrDup("Directory for dobson cartesian radar data files - truth");
  tt->val_offset = (char *) &(pp.truth_data_dir) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("none");
  tt++;
  
  /* Parameter 'truth_label' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("truth_label");
  tt->descr = tdrpStrDup("Truth label");
  tt->help = tdrpStrDup("Label for truth data");
  tt->val_offset = (char *) &(pp.truth_label) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Truth");
  tt++;
  
  /* Parameter 'detect_label' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("detect_label");
  tt->descr = tdrpStrDup("Detection label");
  tt->help = tdrpStrDup("Label for detection data");
  tt->val_offset = (char *) &(pp.detect_label) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Detection");
  tt++;
  
  /* Parameter 'truth_field' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("truth_field");
  tt->descr = tdrpStrDup("Truth field number");
  tt->help = tdrpStrDup("Field number of truth data in cartesian grid (0-based)");
  tt->val_offset = (char *) &(pp.truth_field) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'detect_field' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("detect_field");
  tt->descr = tdrpStrDup("Detection field number");
  tt->help = tdrpStrDup("Field number of detection data in cartesian grid (0-based)");
  tt->val_offset = (char *) &(pp.detect_field) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'output_scan_cont' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("output_scan_cont");
  tt->descr = tdrpStrDup("Option to output scan-by-scan cont. data to named dir");
  tt->help = tdrpStrDup("If set, the contingency table data computed for each scan will be output to a file in the named directory. The file name will comprise the date and the specified file extension");
  tt->val_offset = (char *) &(pp.output_scan_cont) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'scan_cont_dir' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("scan_cont_dir");
  tt->descr = tdrpStrDup("Dir path for scan-by-scan contingency data");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.scan_cont_dir) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("null");
  tt++;
  
  /* Parameter 'scan_cont_ext' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("scan_cont_ext");
  tt->descr = tdrpStrDup("Extension for scan-by-scan contingency data");
  tt->help = tdrpStrDup("");
  tt->val_offset = (char *) &(pp.scan_cont_ext) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("cont");
  tt++;
  
  /* Parameter 'time_lag' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("time_lag");
  tt->descr = tdrpStrDup("Time lag (secs)");
  tt->help = tdrpStrDup("Time lag between truth and detection (secs)");
  tt->val_offset = (char *) &(pp.time_lag) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'time_margin' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("time_margin");
  tt->descr = tdrpStrDup("Time margin (secs)");
  tt->help = tdrpStrDup("Margin around actual file time used when determining time match");
  tt->val_offset = (char *) &(pp.time_margin) - (char *) &pp;
  tt->single_val.l = 180;
  tt++;
  
  /* Parameter 'relaxed_contingency' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("relaxed_contingency");
  tt->descr = tdrpStrDup("Option to use a relaxed algorithm to compute the contingencies");
  tt->help = tdrpStrDup("If set, a relaxed algorithm is used. This means that surrounding points are used in determining the contingency values. The contingency_search_margin governs the search limits. See the code for details");
  tt->val_offset = (char *) &(pp.relaxed_contingency) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'relaxed_margin' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("relaxed_margin");
  tt->descr = tdrpStrDup("Search margin (grid units)");
  tt->help = tdrpStrDup("Number of adjacent grids searched for contingency match");
  tt->val_offset = (char *) &(pp.relaxed_margin) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'check_range' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("check_range");
  tt->descr = tdrpStrDup("Option to check range from radar");
  tt->help = tdrpStrDup("If set, the range from the radar is checked. Only points at less than the maximum range are used");
  tt->val_offset = (char *) &(pp.check_range) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'max_range' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("max_range");
  tt->descr = tdrpStrDup("Max range (km)");
  tt->help = tdrpStrDup("Max. range (km) for analysis - data beyond this is ignored");
  tt->val_offset = (char *) &(pp.max_range) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = 10;
  tt->max_val.d = 1000;
  tt->single_val.d = 150;
  tt++;
  
  /* Parameter 'min_regression_val' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("min_regression_val");
  tt->descr = tdrpStrDup("Min regression data value");
  tt->help = tdrpStrDup("Only points with data above this value are included in the regresion analysis");
  tt->val_offset = (char *) &(pp.min_regression_val) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = -1000;
  tt->max_val.d = 1000;
  tt->single_val.d = 1;
  tt++;
  
  /* Parameter 'truth_level_lower' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("truth_level_lower");
  tt->descr = tdrpStrDup("Lower value for truth level");
  tt->help = tdrpStrDup("Only include truth data above this value in the contingecy table analysis");
  tt->val_offset = (char *) &(pp.truth_level_lower) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = -1000;
  tt->max_val.d = 1000;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'truth_level_upper' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("truth_level_upper");
  tt->descr = tdrpStrDup("Upper value for truth level");
  tt->help = tdrpStrDup("Only include truth data below this value in the contingecy table analysis");
  tt->val_offset = (char *) &(pp.truth_level_upper) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = -1000;
  tt->max_val.d = 1000;
  tt->single_val.d = 100;
  tt++;
  
  /* Parameter 'detect_level_lower' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("detect_level_lower");
  tt->descr = tdrpStrDup("Lower value for detection level");
  tt->help = tdrpStrDup("Only include detection data above this value in the contingecy table analysis");
  tt->val_offset = (char *) &(pp.detect_level_lower) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = -1000;
  tt->max_val.d = 1000;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'detect_level_upper' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("detect_level_upper");
  tt->descr = tdrpStrDup("Upper value for detection level");
  tt->help = tdrpStrDup("Only include detection data below this value in the contingecy table analysis");
  tt->val_offset = (char *) &(pp.detect_level_upper) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = -1000;
  tt->max_val.d = 1000;
  tt->single_val.d = 100;
  tt++;
  
  /* Parameter 'hist' */
  /* ctype is 'verify_grid_hist' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("hist");
  tt->descr = tdrpStrDup("Histogram parameters");
  tt->help = tdrpStrDup("The params for the histogram mode");
  tt->val_offset = (char *) &(pp.hist) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("hist");
  tt->struct_def.nfields = 3;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("long");
    tt->struct_def.fields[0].fname = tdrpStrDup("n_intervals");
    tt->struct_def.fields[0].ptype = LONG_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.hist.n_intervals) - (char *) &(pp.hist);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("low_limit");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.hist.low_limit) - (char *) &(pp.hist);
    tt->struct_def.fields[2].ftype = tdrpStrDup("double");
    tt->struct_def.fields[2].fname = tdrpStrDup("interval_size");
    tt->struct_def.fields[2].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[2].rel_offset = 
      (char *) &(pp.hist.interval_size) - (char *) &(pp.hist);
  tt->n_struct_vals = 3;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].l = 0;
    tt->struct_vals[1].d = 0;
    tt->struct_vals[2].d = 0;
  tt++;
  
  /* Parameter 'use_native_grid' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("use_native_grid");
  tt->descr = tdrpStrDup("Option to use the native grid for the data");
  tt->help = tdrpStrDup("If set, the native data grid is used for evaluation. Otherwise the grid specified in the param file is used");
  tt->val_offset = (char *) &(pp.use_native_grid) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'grid' */
  /* ctype is 'verify_grid_grid' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("grid");
  tt->descr = tdrpStrDup("Grid parameters");
  tt->help = tdrpStrDup("The grid params for the regression mode");
  tt->val_offset = (char *) &(pp.grid) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("grid");
  tt->struct_def.nfields = 6;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("long");
    tt->struct_def.fields[0].fname = tdrpStrDup("nx");
    tt->struct_def.fields[0].ptype = LONG_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.grid.nx) - (char *) &(pp.grid);
    tt->struct_def.fields[1].ftype = tdrpStrDup("long");
    tt->struct_def.fields[1].fname = tdrpStrDup("ny");
    tt->struct_def.fields[1].ptype = LONG_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.grid.ny) - (char *) &(pp.grid);
    tt->struct_def.fields[2].ftype = tdrpStrDup("double");
    tt->struct_def.fields[2].fname = tdrpStrDup("minx");
    tt->struct_def.fields[2].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[2].rel_offset = 
      (char *) &(pp.grid.minx) - (char *) &(pp.grid);
    tt->struct_def.fields[3].ftype = tdrpStrDup("double");
    tt->struct_def.fields[3].fname = tdrpStrDup("miny");
    tt->struct_def.fields[3].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[3].rel_offset = 
      (char *) &(pp.grid.miny) - (char *) &(pp.grid);
    tt->struct_def.fields[4].ftype = tdrpStrDup("double");
    tt->struct_def.fields[4].fname = tdrpStrDup("dx");
    tt->struct_def.fields[4].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[4].rel_offset = 
      (char *) &(pp.grid.dx) - (char *) &(pp.grid);
    tt->struct_def.fields[5].ftype = tdrpStrDup("double");
    tt->struct_def.fields[5].fname = tdrpStrDup("dy");
    tt->struct_def.fields[5].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[5].rel_offset = 
      (char *) &(pp.grid.dy) - (char *) &(pp.grid);
  tt->n_struct_vals = 6;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].l = 0;
    tt->struct_vals[1].l = 0;
    tt->struct_vals[2].d = 0;
    tt->struct_vals[3].d = 0;
    tt->struct_vals[4].d = 0;
    tt->struct_vals[5].d = 0;
  tt++;
  
  /* Parameter 'output_intermediate_grids' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("output_intermediate_grids");
  tt->descr = tdrpStrDup("Option to output intermediate grids to an MDV file");
  tt->help = tdrpStrDup("If set, the intermediate grids are output in an MDV file.  The intermediate grids are the truth and detection grids mapped onto the specified contingency grid and set to 1 if the input grid is between the specified limits, 0 otherwise.  The intermediate grids will only be output if this is set and use_native_grid and relaxed_contingency are both FALSE and you are in CONT mode.");
  tt->val_offset = (char *) &(pp.output_intermediate_grids) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'intermediate_dir' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("intermediate_dir");
  tt->descr = tdrpStrDup("Intermediate grid output directory");
  tt->help = tdrpStrDup("The directory to be used for intermediate grids.  Only used if output_intermediate_grids is TRUE.  The files in this directory are named YYYYMMDD/hhmmss.mdv using the centroid time of the truth file as the time used in naming the file.");
  tt->val_offset = (char *) &(pp.intermediate_dir) - (char *) &pp;
  tt->single_val.s = tdrpStrDup(".");
  tt++;
  
  /* Parameter 'projection' */
  /* ctype is 'verify_grid_projection_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("projection");
  tt->descr = tdrpStrDup("Projection type for intermediate grid file");
  tt->help = tdrpStrDup("Projection type to put in intermediate grid file so it can be displayed.  See output_intermediate_grids parameter.");
  tt->val_offset = (char *) &(pp.projection) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("projection_t");
  tt->enum_def.nfields = 2;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("PROJ_FLAT");
    tt->enum_def.fields[0].val = PROJ_FLAT;
    tt->enum_def.fields[1].name = tdrpStrDup("PROJ_LATLON");
    tt->enum_def.fields[1].val = PROJ_LATLON;
  tt->single_val.e = PROJ_FLAT;
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
