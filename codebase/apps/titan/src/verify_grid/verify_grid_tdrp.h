/*******************************************
 * verify_grid_tdrp.h
 *
 * TDRP header file for 'verify_grid' module.
 *
 * Code for program verify_grid
 *
 * This header file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#ifndef _verify_grid_tdrp_h
#define _verify_grid_tdrp_h

#ifdef __cplusplus
extern "C" {
#endif

#include <tdrp/tdrp.h>

/*
 * typedefs
 */

typedef enum {
  DEBUG_OFF = 0,
  DEBUG_WARNINGS = 1,
  DEBUG_NORM = 2,
  DEBUG_EXTRA = 3
} verify_grid_debug;

typedef enum {
  CONT = 0,
  STATS = 1,
  REGRESSION = 2
} verify_grid_mode;

typedef enum {
  PROJ_FLAT = 0,
  PROJ_LATLON = 1
} verify_grid_projection_t;

typedef struct {
  long n_intervals;
  double low_limit;
  double interval_size;
} verify_grid_hist;

typedef struct {
  long nx;
  long ny;
  double minx;
  double miny;
  double dx;
  double dy;
} verify_grid_grid;

/*
 * typedef for main struct - verify_grid_tdrp_struct
 */

typedef struct {

  size_t struct_size;

  /***** debug *****/

  verify_grid_debug debug;

  /***** malloc_debug_level *****/

  long malloc_debug_level;

  /***** mode *****/

  verify_grid_mode mode;

  /***** truth_data_dir *****/

  char* truth_data_dir;

  /***** truth_label *****/

  char* truth_label;

  /***** detect_label *****/

  char* detect_label;

  /***** truth_field *****/

  long truth_field;

  /***** detect_field *****/

  long detect_field;

  /***** output_scan_cont *****/

  tdrp_bool_t output_scan_cont;

  /***** scan_cont_dir *****/

  char* scan_cont_dir;

  /***** scan_cont_ext *****/

  char* scan_cont_ext;

  /***** time_lag *****/

  long time_lag;

  /***** time_margin *****/

  long time_margin;

  /***** relaxed_contingency *****/

  tdrp_bool_t relaxed_contingency;

  /***** relaxed_margin *****/

  long relaxed_margin;

  /***** check_range *****/

  tdrp_bool_t check_range;

  /***** max_range *****/

  double max_range;

  /***** min_regression_val *****/

  double min_regression_val;

  /***** truth_level_lower *****/

  double truth_level_lower;

  /***** truth_level_upper *****/

  double truth_level_upper;

  /***** detect_level_lower *****/

  double detect_level_lower;

  /***** detect_level_upper *****/

  double detect_level_upper;

  /***** hist *****/

  verify_grid_hist hist;

  /***** use_native_grid *****/

  tdrp_bool_t use_native_grid;

  /***** grid *****/

  verify_grid_grid grid;

  /***** output_intermediate_grids *****/

  tdrp_bool_t output_intermediate_grids;

  /***** intermediate_dir *****/

  char* intermediate_dir;

  /***** projection *****/

  verify_grid_projection_t projection;

} verify_grid_tdrp_struct;

/*
 * function prototypes
 */

/*************************************************************
 * verify_grid_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   verify_grid_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int verify_grid_tdrp_load_from_args(int argc, char **argv,
                                verify_grid_tdrp_struct *params,
                                char **override_list,
                                char **params_path_p);

/*************************************************************
 * verify_grid_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than verify_grid_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   verify_grid_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int verify_grid_tdrp_load(char *param_file_path,
                      verify_grid_tdrp_struct *params,
                      char **override_list,
                      int expand_env, int debug);

/*************************************************************
 * verify_grid_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See verify_grid_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int verify_grid_tdrp_load_defaults(verify_grid_tdrp_struct *params,
                               int expand_env);

/*************************************************************
 * verify_grid_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

extern void verify_grid_tdrp_sync(void);

/*************************************************************
 * verify_grid_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

extern void verify_grid_tdrp_print(FILE *out, tdrp_print_mode_t mode);

/*************************************************************
 * verify_grid_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

extern void verify_grid_tdrp_free_all(void);

/*************************************************************
 * verify_grid_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

extern int verify_grid_tdrp_check_all_set(FILE *out);

/*************************************************************
 * verify_grid_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

extern int verify_grid_tdrp_check_is_set(char *param_name);

/*************************************************************
 * verify_grid_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

extern int verify_grid_tdrp_array_realloc(char *param_name,
                              int new_array_n);

/*************************************************************
 * verify_grid_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

extern int verify_grid_tdrp_array2D_realloc(char *param_name,
                                 int new_array_n1,
                                 int new_array_n2);

/*************************************************************
 * verify_grid_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

extern TDRPtable *verify_grid_tdrp_table(void);

/*************************************************************
 * verify_grid_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

extern TDRPtable *verify_grid_tdrp_init(verify_grid_tdrp_struct *params);

#ifdef __cplusplus
}
#endif

#endif

