/*******************************************
 * DistFit_tdrp.c
 *
 * TDRP C code file 'DistFit' module.
 *
 * Code for program DistFit
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "DistFit_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[23];
static DistFit_tdrp_struct *Params;
static char *Module = "DistFit";

/*************************************************************
 * DistFit_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   DistFit_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int DistFit_tdrp_load_from_args(int argc, char **argv,
                         DistFit_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  DistFit_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * DistFit_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than DistFit_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   DistFit_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int DistFit_tdrp_load(char *param_file_path,
               DistFit_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  DistFit_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * DistFit_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See DistFit_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int DistFit_tdrp_load_defaults(DistFit_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  DistFit_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * DistFit_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void DistFit_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * DistFit_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void DistFit_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * DistFit_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int DistFit_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * DistFit_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int DistFit_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * DistFit_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void DistFit_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * DistFit_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int DistFit_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * DistFit_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int DistFit_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * DistFit_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *DistFit_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * DistFit_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *DistFit_tdrp_init(DistFit_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  DistFit_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(DistFit_tdrp_struct));
  params->struct_size = sizeof(DistFit_tdrp_struct);

  /* Parameter 'debug' */
  /* ctype is 'DistFit_debug' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("Debug option");
  tt->help = tdrpStrDup("If set, debug messages will be printed appropriately");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("debug");
  tt->enum_def.nfields = 3;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
    tt->enum_def.fields[0].val = DEBUG_OFF;
    tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_NORM");
    tt->enum_def.fields[1].val = DEBUG_NORM;
    tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_VERBOSE");
    tt->enum_def.fields[2].val = DEBUG_VERBOSE;
  tt->single_val.e = DEBUG_OFF;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("Malloc debug level");
  tt->help = tdrpStrDup("0 - none, 1 - corruption checking, 2 - records all malloc blocks and checks, 3 - printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("instance");
  tt->descr = tdrpStrDup("Process instance");
  tt->help = tdrpStrDup("Used for registration with procmap.");
  tt->val_offset = (char *) &(pp.instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'distribution_name' */
  /* ctype is 'DistFit_distribution_name' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("distribution_name");
  tt->descr = tdrpStrDup("Name of distribution");
  tt->help = tdrpStrDup("Name of distribution to be fitted to the data. If DIST_ALL, all relevant distributions will be fitted.\n");
  tt->val_offset = (char *) &(pp.distribution_name) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("distribution_name");
  tt->enum_def.nfields = 5;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DIST_ALL");
    tt->enum_def.fields[0].val = DIST_ALL;
    tt->enum_def.fields[1].name = tdrpStrDup("DIST_NORMAL");
    tt->enum_def.fields[1].val = DIST_NORMAL;
    tt->enum_def.fields[2].name = tdrpStrDup("DIST_WEIBULL");
    tt->enum_def.fields[2].val = DIST_WEIBULL;
    tt->enum_def.fields[3].name = tdrpStrDup("DIST_EXPONENTIAL");
    tt->enum_def.fields[3].val = DIST_EXPONENTIAL;
    tt->enum_def.fields[4].name = tdrpStrDup("DIST_GAMMA");
    tt->enum_def.fields[4].val = DIST_GAMMA;
  tt->single_val.e = DIST_ALL;
  tt++;
  
  /* Parameter 'distribution_class' */
  /* ctype is 'DistFit_distribution_class' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("distribution_class");
  tt->descr = tdrpStrDup("Class of distribution");
  tt->help = tdrpStrDup("If UNI_VARIATE, the x data is fitted. If BI_VARIATE, x and y data form the variates.");
  tt->val_offset = (char *) &(pp.distribution_class) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("distribution_class");
  tt->enum_def.nfields = 2;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("UNI_VARIATE");
    tt->enum_def.fields[0].val = UNI_VARIATE;
    tt->enum_def.fields[1].name = tdrpStrDup("BI_VARIATE");
    tt->enum_def.fields[1].val = BI_VARIATE;
  tt->single_val.e = UNI_VARIATE;
  tt++;
  
  /* Parameter 'chisq_nbins' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("chisq_nbins");
  tt->descr = tdrpStrDup("Number of bins for chisq parameter.");
  tt->help = tdrpStrDup("When computing the chisq parameter, this is the number of intervals used for binning the data.");
  tt->val_offset = (char *) &(pp.chisq_nbins) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.l = 2;
  tt->single_val.l = 20;
  tt++;
  
  /* Parameter 'x_label' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("x_label");
  tt->descr = tdrpStrDup("Label for X data");
  tt->help = tdrpStrDup("Used to identify column for X data.");
  tt->val_offset = (char *) &(pp.x_label) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("X");
  tt++;
  
  /* Parameter 'y_label' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("y_label");
  tt->descr = tdrpStrDup("Label for Y data");
  tt->help = tdrpStrDup("Used to identify column for Y data.");
  tt->val_offset = (char *) &(pp.y_label) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Y");
  tt++;
  
  /* Parameter 'condition_input_data' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("condition_input_data");
  tt->descr = tdrpStrDup("Apply conditions to input data.");
  tt->help = tdrpStrDup("If TRUE, the input data values are conditioned on a given field, which lie in a given intervals - see cond_min, cond_max.");
  tt->val_offset = (char *) &(pp.condition_input_data) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'conditional_label' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("conditional_label");
  tt->descr = tdrpStrDup("Label for conditional data");
  tt->help = tdrpStrDup("Used to identify column for conditional data.");
  tt->val_offset = (char *) &(pp.conditional_label) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("cond");
  tt++;
  
  /* Parameter 'cond_min' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("cond_min");
  tt->descr = tdrpStrDup("Min value for conditional field.");
  tt->help = tdrpStrDup("See condition_input_data.");
  tt->val_offset = (char *) &(pp.cond_min) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'cond_max' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("cond_max");
  tt->descr = tdrpStrDup("Max value for conditional field.");
  tt->help = tdrpStrDup("See condition_input_data.");
  tt->val_offset = (char *) &(pp.cond_max) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'perform_attrition' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("perform_attrition");
  tt->descr = tdrpStrDup("Perform attrition on the input data.");
  tt->help = tdrpStrDup("If TRUE, only every n data points will be used, where n is the attrition_count.");
  tt->val_offset = (char *) &(pp.perform_attrition) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'attrition_count' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("attrition_count");
  tt->descr = tdrpStrDup("Attrition count.");
  tt->help = tdrpStrDup("When perform_attrition is TRUE, this is the count used to save data. Every count point is saved.");
  tt->val_offset = (char *) &(pp.attrition_count) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.l = 2;
  tt->single_val.l = 5;
  tt++;
  
  /* Parameter 'log_x_data' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("log_x_data");
  tt->descr = tdrpStrDup("Transform x data using log.");
  tt->help = tdrpStrDup("If TRUE, x data will become log(x).");
  tt->val_offset = (char *) &(pp.log_x_data) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'limit_x_data' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("limit_x_data");
  tt->descr = tdrpStrDup("Apply limits to x data.");
  tt->help = tdrpStrDup("If TRUE, the input data values are limited to between x_min and x_max.");
  tt->val_offset = (char *) &(pp.limit_x_data) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'x_min' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("x_min");
  tt->descr = tdrpStrDup("Min value for x field.");
  tt->help = tdrpStrDup("See limit_x_data.");
  tt->val_offset = (char *) &(pp.x_min) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'x_max' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("x_max");
  tt->descr = tdrpStrDup("Max value for x field.");
  tt->help = tdrpStrDup("See limit_x_data.");
  tt->val_offset = (char *) &(pp.x_max) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'log_y_data' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("log_y_data");
  tt->descr = tdrpStrDup("Transform y data using log.");
  tt->help = tdrpStrDup("If TRUE, y data will become log(y).");
  tt->val_offset = (char *) &(pp.log_y_data) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'limit_y_data' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("limit_y_data");
  tt->descr = tdrpStrDup("Apply limits to y data.");
  tt->help = tdrpStrDup("If TRUE, the input data values are limited to between y_min and y_max.");
  tt->val_offset = (char *) &(pp.limit_y_data) - (char *) &pp;
  tt->single_val.b = pFALSE;
  tt++;
  
  /* Parameter 'y_min' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("y_min");
  tt->descr = tdrpStrDup("Min value for y field.");
  tt->help = tdrpStrDup("See limit_y_data.");
  tt->val_offset = (char *) &(pp.y_min) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'y_max' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("y_max");
  tt->descr = tdrpStrDup("Max value for y field.");
  tt->help = tdrpStrDup("See limit_y_data.");
  tt->val_offset = (char *) &(pp.y_max) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
