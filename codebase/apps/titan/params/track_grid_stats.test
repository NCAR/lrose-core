/*********************************************************
 * run-time parameters for track_grid_stats
 *
 * Mike Dixon, RAP, NCXAR, Boulder, CO, USA, 80307-3000
 *
 * Sept 1993
 *	
 * See paramdef.track_grid_stats in src directory for
 * details on the parameters.
 */

debug = DEBUG_OFF;
malloc_debug_level = 2;
note = "Radar track stats, MHR, Denver, CO";

grid_stats_dir = "/home/dixon/titan/grid_stats2";

scan_interval = 360.0;
min_duration = 1200.0;
	
n_seasons = 3;

/*
 * grid params - {nx, ny, minx, miny, dx, dy}
 */

grid = {120, 120, -148.75, -148.75, 2.5, 2.5};

/*
 * parameters for dur_max_depth computations
 */

compute_dur_max_precip = TRUE;
dur_for_max_precip = 3600;

z_r_coeff = 500.0;
z_r_exponent = 1.3;
hail_dbz_threshold = 50.0;

