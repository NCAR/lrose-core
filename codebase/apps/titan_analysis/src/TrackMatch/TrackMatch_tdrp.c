/*******************************************
 * TrackMatch_tdrp.c
 *
 * TDRP C code file 'TrackMatch' module.
 *
 * Code for program TrackMatch
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "TrackMatch_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[11];
static TrackMatch_tdrp_struct *Params;
static char *Module = "TrackMatch";

/*************************************************************
 * TrackMatch_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   TrackMatch_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int TrackMatch_tdrp_load_from_args(int argc, char **argv,
                         TrackMatch_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  TrackMatch_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * TrackMatch_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than TrackMatch_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   TrackMatch_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int TrackMatch_tdrp_load(char *param_file_path,
               TrackMatch_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  TrackMatch_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * TrackMatch_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See TrackMatch_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int TrackMatch_tdrp_load_defaults(TrackMatch_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  TrackMatch_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * TrackMatch_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void TrackMatch_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * TrackMatch_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void TrackMatch_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * TrackMatch_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int TrackMatch_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * TrackMatch_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int TrackMatch_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * TrackMatch_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void TrackMatch_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * TrackMatch_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int TrackMatch_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * TrackMatch_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int TrackMatch_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * TrackMatch_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *TrackMatch_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * TrackMatch_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *TrackMatch_tdrp_init(TrackMatch_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  TrackMatch_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(TrackMatch_tdrp_struct));
  params->struct_size = sizeof(TrackMatch_tdrp_struct);

  /* Parameter 'debug' */
  /* ctype is 'TrackMatch_debug' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("Debug option");
  tt->help = tdrpStrDup("If set, debug messages will be printed with the appropriate level of detail.");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("debug");
  tt->enum_def.nfields = 4;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
    tt->enum_def.fields[0].val = DEBUG_OFF;
    tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_WARNINGS");
    tt->enum_def.fields[1].val = DEBUG_WARNINGS;
    tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_NORM");
    tt->enum_def.fields[2].val = DEBUG_NORM;
    tt->enum_def.fields[3].name = tdrpStrDup("DEBUG_VERBOSE");
    tt->enum_def.fields[3].val = DEBUG_VERBOSE;
  tt->single_val.e = DEBUG_OFF;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("Malloc debug level");
  tt->help = tdrpStrDup("0 - none, 1 - corruption checking, 2 - records all malloc blocks and checks, 3 - printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("instance");
  tt->descr = tdrpStrDup("Process instance");
  tt->help = tdrpStrDup("Used for registration with procmap.");
  tt->val_offset = (char *) &(pp.instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'case_num' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("case_num");
  tt->descr = tdrpStrDup("Case number for the match.");
  tt->help = tdrpStrDup("This is the case for which the match is sought.");
  tt->val_offset = (char *) &(pp.case_num) - (char *) &pp;
  tt->single_val.l = 1;
  tt++;
  
  /* Parameter 'case_file_path' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("case_file_path");
  tt->descr = tdrpStrDup("File path of seed cases.");
  tt->help = tdrpStrDup("This file indicates the time and track numbers for each seeded case. In addition the environmental conditions, such as cloud base and CAPE are input from this file.");
  tt->val_offset = (char *) &(pp.case_file_path) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("null");
  tt++;
  
  /* Parameter 'n_candidates' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("n_candidates");
  tt->descr = tdrpStrDup("Number of track candidates in list.");
  tt->help = tdrpStrDup("This program finds tracks which match the given case as closely as possible. A list of candidate tracks is found and sorted. The list is n_candidates long.");
  tt->val_offset = (char *) &(pp.n_candidates) - (char *) &pp;
  tt->single_val.l = 20;
  tt++;
  
  /* Parameter 'match_property' */
  /* ctype is 'TrackMatch_match_property' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("match_property");
  tt->descr = tdrpStrDup("Property for matching tracks.");
  tt->help = tdrpStrDup("This is the property used for matching up the tracks.");
  tt->val_offset = (char *) &(pp.match_property) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("match_property");
  tt->enum_def.nfields = 4;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("VOLUME");
    tt->enum_def.fields[0].val = VOLUME;
    tt->enum_def.fields[1].name = tdrpStrDup("AREA");
    tt->enum_def.fields[1].val = AREA;
    tt->enum_def.fields[2].name = tdrpStrDup("MASS");
    tt->enum_def.fields[2].val = MASS;
    tt->enum_def.fields[3].name = tdrpStrDup("PRECIP_FLUX");
    tt->enum_def.fields[3].val = PRECIP_FLUX;
  tt->single_val.e = PRECIP_FLUX;
  tt++;
  
  /* Parameter 'use_rate_for_match' */
  /* ctype is 'tdrp_bool_t' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = BOOL_TYPE;
  tt->param_name = tdrpStrDup("use_rate_for_match");
  tt->descr = tdrpStrDup("Option to use rate for matching.");
  tt->help = tdrpStrDup("If set, the rate of change will be used for matching. If not, the absolute value will be used for the match.");
  tt->val_offset = (char *) &(pp.use_rate_for_match) - (char *) &pp;
  tt->single_val.b = pTRUE;
  tt++;
  
  /* Parameter 'time_margin' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("time_margin");
  tt->descr = tdrpStrDup("Margin between time of case track and candidate track (hr).");
  tt->help = tdrpStrDup("Only tracks with a start time difference within this margin are considered. Use -1.0 for no checking.");
  tt->val_offset = (char *) &(pp.time_margin) - (char *) &pp;
  tt->single_val.d = 2;
  tt++;
  
  /* Parameter 'range_margin' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("range_margin");
  tt->descr = tdrpStrDup("Margin between range of case track and candidate track (km).");
  tt->help = tdrpStrDup("Only tracks with a start range difference within this margin are considered. Use -1.0 for no checking.");
  tt->val_offset = (char *) &(pp.range_margin) - (char *) &pp;
  tt->single_val.d = 25;
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
