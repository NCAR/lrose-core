/*******************************************
 * DistFit_tdrp.h
 *
 * TDRP header file for 'DistFit' module.
 *
 * Code for program DistFit
 *
 * This header file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#ifndef _DistFit_tdrp_h
#define _DistFit_tdrp_h

#ifdef __cplusplus
extern "C" {
#endif

#include <tdrp/tdrp.h>

/*
 * typedefs
 */

typedef enum {
  DEBUG_OFF = 0,
  DEBUG_NORM = 1,
  DEBUG_VERBOSE = 2
} DistFit_debug;

typedef enum {
  DIST_ALL = 0,
  DIST_NORMAL = 1,
  DIST_WEIBULL = 2,
  DIST_EXPONENTIAL = 3,
  DIST_GAMMA = 4
} DistFit_distribution_name;

typedef enum {
  UNI_VARIATE = 0,
  BI_VARIATE = 1
} DistFit_distribution_class;

/*
 * typedef for main struct - DistFit_tdrp_struct
 */

typedef struct {

  size_t struct_size;

  /***** debug *****/

  DistFit_debug debug;

  /***** malloc_debug_level *****/

  long malloc_debug_level;

  /***** instance *****/

  char* instance;

  /***** distribution_name *****/

  DistFit_distribution_name distribution_name;

  /***** distribution_class *****/

  DistFit_distribution_class distribution_class;

  /***** chisq_nbins *****/

  long chisq_nbins;

  /***** x_label *****/

  char* x_label;

  /***** y_label *****/

  char* y_label;

  /***** condition_input_data *****/

  tdrp_bool_t condition_input_data;

  /***** conditional_label *****/

  char* conditional_label;

  /***** cond_min *****/

  double cond_min;

  /***** cond_max *****/

  double cond_max;

  /***** perform_attrition *****/

  tdrp_bool_t perform_attrition;

  /***** attrition_count *****/

  long attrition_count;

  /***** log_x_data *****/

  tdrp_bool_t log_x_data;

  /***** limit_x_data *****/

  tdrp_bool_t limit_x_data;

  /***** x_min *****/

  double x_min;

  /***** x_max *****/

  double x_max;

  /***** log_y_data *****/

  tdrp_bool_t log_y_data;

  /***** limit_y_data *****/

  tdrp_bool_t limit_y_data;

  /***** y_min *****/

  double y_min;

  /***** y_max *****/

  double y_max;

} DistFit_tdrp_struct;

/*
 * function prototypes
 */

/*************************************************************
 * DistFit_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   DistFit_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int DistFit_tdrp_load_from_args(int argc, char **argv,
                                DistFit_tdrp_struct *params,
                                char **override_list,
                                char **params_path_p);

/*************************************************************
 * DistFit_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than DistFit_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   DistFit_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int DistFit_tdrp_load(char *param_file_path,
                      DistFit_tdrp_struct *params,
                      char **override_list,
                      int expand_env, int debug);

/*************************************************************
 * DistFit_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See DistFit_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

extern int DistFit_tdrp_load_defaults(DistFit_tdrp_struct *params,
                               int expand_env);

/*************************************************************
 * DistFit_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

extern void DistFit_tdrp_sync(void);

/*************************************************************
 * DistFit_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

extern void DistFit_tdrp_print(FILE *out, tdrp_print_mode_t mode);

/*************************************************************
 * DistFit_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

extern void DistFit_tdrp_free_all(void);

/*************************************************************
 * DistFit_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

extern int DistFit_tdrp_check_all_set(FILE *out);

/*************************************************************
 * DistFit_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

extern int DistFit_tdrp_check_is_set(char *param_name);

/*************************************************************
 * DistFit_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

extern int DistFit_tdrp_array_realloc(char *param_name,
                              int new_array_n);

/*************************************************************
 * DistFit_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

extern int DistFit_tdrp_array2D_realloc(char *param_name,
                                 int new_array_n1,
                                 int new_array_n2);

/*************************************************************
 * DistFit_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

extern TDRPtable *DistFit_tdrp_table(void);

/*************************************************************
 * DistFit_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

extern TDRPtable *DistFit_tdrp_init(DistFit_tdrp_struct *params);

#ifdef __cplusplus
}
#endif

#endif

