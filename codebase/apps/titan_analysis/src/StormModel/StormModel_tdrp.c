/*******************************************
 * StormModel_tdrp.c
 *
 * TDRP C code file 'StormModel' module.
 *
 * Code for program StormModel
 *
 * This file has been automatically
 * generated by TDRP, do not modify.
 *
 *******************************************/

#include "StormModel_tdrp.h"
#include <string.h>

/*
 * file scope variables
 */

static TDRPtable Table[35];
static StormModel_tdrp_struct *Params;
static char *Module = "StormModel";

/*************************************************************
 * StormModel_tdrp_load_from_args()
 *
 * Loads up TDRP using the command line args.
 *
 * Check TDRP_usage() for command line actions associated with
 * this function.
 *
 *   argc, argv: command line args
 *
 *   StormModel_tdrp_struct *params: loads up this struct
 * 
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *   char **params_path_p: if non-NULL, this is set to point to
 *                         the path of the params file used.
 *
 *  Returns 0 on success, -1 on failure.
 */

int StormModel_tdrp_load_from_args(int argc, char **argv,
                         StormModel_tdrp_struct *params,
                         char **override_list,
                         char **params_path_p)
{
  Params = params;
  StormModel_tdrp_init(Params);
  if (tdrpLoadFromArgs(argc, argv,
                       Table, Params,
                       override_list, params_path_p)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * StormModel_tdrp_load()
 *
 * Loads up TDRP for a given module.
 *
 * This version of load gives the programmer the option to load
 * up more than one module for a single application. It is a
 * lower-level routine than StormModel_tdrp_load_from_args,
 * and hence more flexible, but the programmer must do more work.
 *
 *   char *param_file_path: the parameter file to be read in.
 *
 *   StormModel_tdrp_struct *params: loads up this struct
 *
 *   char **override_list: A null-terminated list of overrides
 *     to the parameter file.
 *     An override string has exactly the format of an entry
 *     in the parameter file itself.
 *
 *  expand_env: flag to control environment variable
 *                expansion during tokenization.
 *              If TRUE, environment expansion is set on.
 *              If FALSE, environment expansion is set off.
 *
 *  Returns 0 on success, -1 on failure.
 */

int StormModel_tdrp_load(char *param_file_path,
               StormModel_tdrp_struct *params,
               char **override_list,
               int expand_env, int debug)
{
  Params = params;
  StormModel_tdrp_init(Params);
  if (tdrpLoad(param_file_path, Table,
               params, override_list,
               expand_env, debug)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * StormModel_tdrp_load_defaults()
 *
 * Loads up defaults for a given module.
 *
 * See StormModel_tdrp_load() for more details.
 *
 *  Returns 0 on success, -1 on failure.
 */

int StormModel_tdrp_load_defaults(StormModel_tdrp_struct *params,
                        int expand_env)
{
  Params = params;
  StormModel_tdrp_init(Params);
  if (tdrpLoad(NULL, Table,
               params, NULL,
               expand_env, FALSE)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * StormModel_tdrp_sync()
 *
 * Syncs the user struct data back into the parameter table,
 * in preparation for printing.
 */

void StormModel_tdrp_sync(void)
{
  tdrpUser2Table(Table, Params);
}

/*************************************************************
 * StormModel_tdrp_print()
 * 
 * Print params file
 *
 * The modes supported are:
 *
 *   PRINT_SHORT:   main comments only, no help or descriptions
 *                  structs and arrays on a single line
 *   PRINT_NORM:    short + descriptions and help
 *   PRINT_LONG:    norm  + arrays and structs expanded
 *   PRINT_VERBOSE: long  + private params included
 */

void StormModel_tdrp_print(FILE *out, tdrp_print_mode_t mode)
{
  tdrpPrint(out, Table, Module, mode);
}

/*************************************************************
 * StormModel_tdrp_check_all_set()
 *
 * Return TRUE if all set, FALSE if not.
 *
 * If out is non-NULL, prints out warning messages for those
 * parameters which are not set.
 */

int StormModel_tdrp_check_all_set(FILE *out)
{
  return (tdrpCheckAllSet(out, Table, Params));
}

/*************************************************************
 * StormModel_tdrp_check_is_set()
 *
 * Return TRUE if parameter is set, FALSE if not.
 *
 */

int StormModel_tdrp_check_is_set(char *param_name)
{
  return (tdrpCheckIsSet(param_name, Table, Params));
}

/*************************************************************
 * StormModel_tdrp_free_all()
 *
 * Frees up all TDRP dynamic memory.
 */

void StormModel_tdrp_free_all(void)
{
  tdrpFreeAll(Table, Params);
}

/*************************************************************
 * StormModel_tdrp_array_realloc()
 *
 * Realloc 1D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int StormModel_tdrp_array_realloc(char *param_name, int new_array_n)
{
  if (tdrpArrayRealloc(Table, Params, param_name, new_array_n)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * StormModel_tdrp_array2D_realloc()
 *
 * Realloc 2D array.
 *
 * If size is increased, the values from the last array entry is
 * copied into the new space.
 *
 * Returns 0 on success, -1 on error.
 */

int StormModel_tdrp_array2D_realloc(char *param_name,
                          int new_array_n1,
                          int new_array_n2)
{
  if (tdrpArray2DRealloc(Table, Params, param_name,
			 new_array_n1, new_array_n2)) {
    return (-1);
  } else {
    return (0);
  }
}

/*************************************************************
 * StormModel_tdrp_table()
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *StormModel_tdrp_table(void)
{
  return (Table);
}

/*************************************************************
 * StormModel_tdrp_init()
 *
 * Module table initialization function.
 *
 *
 * Returns pointer to static Table for this module.
 */

TDRPtable *StormModel_tdrp_init(StormModel_tdrp_struct *params)

{

  TDRPtable *tt = Table;

  StormModel_tdrp_struct pp; /* for computing byte_offsets */

  /* zero out struct, and store size */

  memset(params, 0, sizeof(StormModel_tdrp_struct));
  params->struct_size = sizeof(StormModel_tdrp_struct);

  /* Parameter 'debug' */
  /* ctype is 'StormModel_debug' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = ENUM_TYPE;
  tt->param_name = tdrpStrDup("debug");
  tt->descr = tdrpStrDup("Debug option");
  tt->help = tdrpStrDup("If set, debug messages will be printed appropriately");
  tt->val_offset = (char *) &(pp.debug) - (char *) &pp;
  tt->enum_def.name = tdrpStrDup("debug");
  tt->enum_def.nfields = 3;
  tt->enum_def.fields = (enum_field_t *)
      tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
    tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
    tt->enum_def.fields[0].val = DEBUG_OFF;
    tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_NORM");
    tt->enum_def.fields[1].val = DEBUG_NORM;
    tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_VERBOSE");
    tt->enum_def.fields[2].val = DEBUG_VERBOSE;
  tt->single_val.e = DEBUG_OFF;
  tt++;
  
  /* Parameter 'malloc_debug_level' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("malloc_debug_level");
  tt->descr = tdrpStrDup("Malloc debug level");
  tt->help = tdrpStrDup("0 - none, 1 - corruption checking, 2 - records all malloc blocks and checks, 3 - printout of all mallocs etc.");
  tt->val_offset = (char *) &(pp.malloc_debug_level) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.l = 0;
  tt->max_val.l = 3;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'instance' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("instance");
  tt->descr = tdrpStrDup("Process instance");
  tt->help = tdrpStrDup("Used for registration with procmap.");
  tt->val_offset = (char *) &(pp.instance) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("Test");
  tt++;
  
  /* Parameter 'nstorms_gen' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("nstorms_gen");
  tt->descr = tdrpStrDup("Number of storms generated.");
  tt->help = tdrpStrDup("StormModel generates a number of artificial storms. This is the number generated.");
  tt->val_offset = (char *) &(pp.nstorms_gen) - (char *) &pp;
  tt->single_val.l = 10000;
  tt++;
  
  /* Parameter 'random_seed' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("random_seed");
  tt->descr = tdrpStrDup("Seed for random number generator.");
  tt->help = tdrpStrDup("If -1, unix time is used as the seed.");
  tt->val_offset = (char *) &(pp.random_seed) - (char *) &pp;
  tt->single_val.l = -1;
  tt++;
  
  /* Parameter 'event_dur_gamma' */
  /* ctype is 'StormModel_event_dur_hist' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("event_dur_gamma");
  tt->descr = tdrpStrDup("Gamma distribution parameters for duration of events.");
  tt->help = tdrpStrDup("This distribution is used to generate the duration of the event periods.");
  tt->val_offset = (char *) &(pp.event_dur_gamma) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("event_dur_hist");
  tt->struct_def.nfields = 2;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("shape");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.event_dur_gamma.shape) - (char *) &(pp.event_dur_gamma);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("scale");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.event_dur_gamma.scale) - (char *) &(pp.event_dur_gamma);
  tt->n_struct_vals = 2;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
  tt++;
  
  /* Parameter 'event_start_tod_hist' */
  /* ctype is 'StormModel_event_start_tod_hist' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("event_start_tod_hist");
  tt->descr = tdrpStrDup("Density histogram for time-of-day of start of events.");
  tt->help = tdrpStrDup("This histogram is used to construct a PDF from which to sample the start tod. The values are in hours (for time) and density (for probability).");
  tt->val_offset = (char *) &(pp.event_start_tod_hist.val) - (char *) &pp;
  tt->len_offset = (char *) &(pp.event_start_tod_hist.len) - (char *) &pp;
  tt->array_offset = (char *) &(pp._event_start_tod_hist) - (char *) &pp;
  tt->array_n_offset = (char *) &(pp.event_start_tod_hist_n) - (char *) &pp;
  tt->is_array = TRUE;
  tt->array_len_fixed = FALSE;
  tt->array_elem_size = sizeof(StormModel_event_start_tod_hist);
  tt->array_n = 1;
  tt->struct_def.name = tdrpStrDup("event_start_tod_hist");
  tt->struct_def.nfields = 2;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("time");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.event_start_tod_hist.val->time) - (char *) pp.event_start_tod_hist.val;
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("prob");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.event_start_tod_hist.val->prob) - (char *) pp.event_start_tod_hist.val;
  tt->n_struct_vals = 2;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
  tt++;
  
  /* Parameter 'event_gap_gamma' */
  /* ctype is 'StormModel_event_gap_gamma' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("event_gap_gamma");
  tt->descr = tdrpStrDup("Gamma distribution parameters for event gap.");
  tt->help = tdrpStrDup("Event gap is the number of hours between end of one event period and the start of the next. It is gamma distributed");
  tt->val_offset = (char *) &(pp.event_gap_gamma) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("event_gap_gamma");
  tt->struct_def.nfields = 2;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("shape");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.event_gap_gamma.shape) - (char *) &(pp.event_gap_gamma);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("scale");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.event_gap_gamma.scale) - (char *) &(pp.event_gap_gamma);
  tt->n_struct_vals = 2;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
  tt++;
  
  /* Parameter 'event_gap_min' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("event_gap_min");
  tt->descr = tdrpStrDup("Minimum event gap (hr)");
  tt->help = tdrpStrDup("This is the minimum event gap. It corresponds to the value of event_gap_max for the program TimeProps. Any gap less than this time is not considered a gap in event, but rather a temporary lull.");
  tt->val_offset = (char *) &(pp.event_gap_min) - (char *) &pp;
  tt->single_val.d = 3;
  tt++;
  
  /* Parameter 'ln_storm_start_gap_norm' */
  /* ctype is 'StormModel_ln_storm_start_gap_norm' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("ln_storm_start_gap_norm");
  tt->descr = tdrpStrDup("Normal distribution parameters for ln of storm start gap.");
  tt->help = tdrpStrDup("Storm start gap is the number of hours between the start of one storm in an event and the start of the next storm. It is lognormally distributed");
  tt->val_offset = (char *) &(pp.ln_storm_start_gap_norm) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("ln_storm_start_gap_norm");
  tt->struct_def.nfields = 2;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("mean");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.ln_storm_start_gap_norm.mean) - (char *) &(pp.ln_storm_start_gap_norm);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("sdev");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.ln_storm_start_gap_norm.sdev) - (char *) &(pp.ln_storm_start_gap_norm);
  tt->n_struct_vals = 2;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
  tt++;
  
  /* Parameter 'storm_start_gap_min' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("storm_start_gap_min");
  tt->descr = tdrpStrDup("Minimum storm start gap (hr)");
  tt->help = tdrpStrDup("This is the minimum storm start gap. It is set non-zero to prevent large numbers of very small start gaps.");
  tt->val_offset = (char *) &(pp.storm_start_gap_min) - (char *) &pp;
  tt->has_max = TRUE;
  tt->max_val.d = 0.1;
  tt->single_val.d = 0.05;
  tt++;
  
  /* Parameter 'Dm_gamma' */
  /* ctype is 'StormModel_Dm_gamma' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("Dm_gamma");
  tt->descr = tdrpStrDup("3-param Gamma distribution parameters for Dm variate.");
  tt->help = tdrpStrDup("Dm is the Duration Measure. It is effectively the 'standard deviation' of the storm Area-time curve when fitted with a Gaussian.");
  tt->val_offset = (char *) &(pp.Dm_gamma) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("Dm_gamma");
  tt->struct_def.nfields = 3;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("shape");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.Dm_gamma.shape) - (char *) &(pp.Dm_gamma);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("scale");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.Dm_gamma.scale) - (char *) &(pp.Dm_gamma);
    tt->struct_def.fields[2].ftype = tdrpStrDup("double");
    tt->struct_def.fields[2].fname = tdrpStrDup("lbound");
    tt->struct_def.fields[2].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[2].rel_offset = 
      (char *) &(pp.Dm_gamma.lbound) - (char *) &(pp.Dm_gamma);
  tt->n_struct_vals = 3;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
    tt->struct_vals[2].d = 0;
  tt++;
  
  /* Parameter 'min_duration' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("min_duration");
  tt->descr = tdrpStrDup("Minimum track duration (hrs).");
  tt->help = tdrpStrDup("This should match the min_duration value in TrackProps. Note however that TrackProps uses secs for units.");
  tt->val_offset = (char *) &(pp.min_duration) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 0.5;
  tt++;
  
  /* Parameter 'max_duration' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("max_duration");
  tt->descr = tdrpStrDup("Maximum track duration (hrs).");
  tt->help = tdrpStrDup("This is the maximum allowable duration. If a longer duration is generated it is rejected and the process repeated.");
  tt->val_offset = (char *) &(pp.max_duration) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 8;
  tt++;
  
  /* Parameter 'max_lnA' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("max_lnA");
  tt->descr = tdrpStrDup("Maximum log of mean area.");
  tt->help = tdrpStrDup("This is the maximum allowable lnA. If a greater lnA is generated it is rejected and the process repeated.");
  tt->val_offset = (char *) &(pp.max_lnA) - (char *) &pp;
  tt->has_min = TRUE;
  tt->min_val.d = 0;
  tt->single_val.d = 9;
  tt++;
  
  /* Parameter 'lnA_vs_Dm' */
  /* ctype is 'StormModel_lnA_vs_Dm' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("lnA_vs_Dm");
  tt->descr = tdrpStrDup("Correlation and distribution parameters for ln(A) as a function of Dm.");
  tt->help = tdrpStrDup("The conditional distribution of ln(A), given Dm, is a 3-parameter gamma. The lnA_vs_Dm array gives the shape, scale and min values for the gamma distribution of ln(A) for set values of Dm. The values for an actual Dm are interpolated from this array.");
  tt->val_offset = (char *) &(pp.lnA_vs_Dm.val) - (char *) &pp;
  tt->len_offset = (char *) &(pp.lnA_vs_Dm.len) - (char *) &pp;
  tt->array_offset = (char *) &(pp._lnA_vs_Dm) - (char *) &pp;
  tt->array_n_offset = (char *) &(pp.lnA_vs_Dm_n) - (char *) &pp;
  tt->is_array = TRUE;
  tt->array_len_fixed = FALSE;
  tt->array_elem_size = sizeof(StormModel_lnA_vs_Dm);
  tt->array_n = 1;
  tt->struct_def.name = tdrpStrDup("lnA_vs_Dm");
  tt->struct_def.nfields = 4;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("Dm");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.lnA_vs_Dm.val->Dm) - (char *) pp.lnA_vs_Dm.val;
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("shape");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.lnA_vs_Dm.val->shape) - (char *) pp.lnA_vs_Dm.val;
    tt->struct_def.fields[2].ftype = tdrpStrDup("double");
    tt->struct_def.fields[2].fname = tdrpStrDup("scale");
    tt->struct_def.fields[2].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[2].rel_offset = 
      (char *) &(pp.lnA_vs_Dm.val->scale) - (char *) pp.lnA_vs_Dm.val;
    tt->struct_def.fields[3].ftype = tdrpStrDup("double");
    tt->struct_def.fields[3].fname = tdrpStrDup("lbound");
    tt->struct_def.fields[3].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[3].rel_offset = 
      (char *) &(pp.lnA_vs_Dm.val->lbound) - (char *) pp.lnA_vs_Dm.val;
  tt->n_struct_vals = 4;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
    tt->struct_vals[2].d = 0;
    tt->struct_vals[3].d = 0;
  tt++;
  
  /* Parameter 'area_threshold' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("area_threshold");
  tt->descr = tdrpStrDup("Area threshold (km2).");
  tt->help = tdrpStrDup("This area is used to compute storm duration form the Gaussian which relates a(t) = f(A, Dm, t).");
  tt->val_offset = (char *) &(pp.area_threshold) - (char *) &pp;
  tt->single_val.d = 10;
  tt++;
  
  /* Parameter 'dBZmax_vs_Dm' */
  /* ctype is 'StormModel_dBZmax_vs_Dm' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("dBZmax_vs_Dm");
  tt->descr = tdrpStrDup("Correlation and distribution parameters for dBZmax as a function of Dm.");
  tt->help = tdrpStrDup("The conditional distribution of dbzMax, given Dm, is normal. The dBZmax_vs_Dm array gives the mean and standard deviation values for dbzMax for set values of Dm. The values for an actual Dm are interpolated from this array.");
  tt->val_offset = (char *) &(pp.dBZmax_vs_Dm.val) - (char *) &pp;
  tt->len_offset = (char *) &(pp.dBZmax_vs_Dm.len) - (char *) &pp;
  tt->array_offset = (char *) &(pp._dBZmax_vs_Dm) - (char *) &pp;
  tt->array_n_offset = (char *) &(pp.dBZmax_vs_Dm_n) - (char *) &pp;
  tt->is_array = TRUE;
  tt->array_len_fixed = FALSE;
  tt->array_elem_size = sizeof(StormModel_dBZmax_vs_Dm);
  tt->array_n = 1;
  tt->struct_def.name = tdrpStrDup("dBZmax_vs_Dm");
  tt->struct_def.nfields = 3;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("Dm");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.dBZmax_vs_Dm.val->Dm) - (char *) pp.dBZmax_vs_Dm.val;
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("mean");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.dBZmax_vs_Dm.val->mean) - (char *) pp.dBZmax_vs_Dm.val;
    tt->struct_def.fields[2].ftype = tdrpStrDup("double");
    tt->struct_def.fields[2].fname = tdrpStrDup("sdev");
    tt->struct_def.fields[2].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[2].rel_offset = 
      (char *) &(pp.dBZmax_vs_Dm.val->sdev) - (char *) pp.dBZmax_vs_Dm.val;
  tt->n_struct_vals = 3;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
    tt->struct_vals[2].d = 0;
  tt++;
  
  /* Parameter 'scan_interval' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("scan_interval");
  tt->descr = tdrpStrDup("Radar vol scan interval (secs)");
  tt->help = tdrpStrDup("Interval between radar volume scans (secs). Used to compute the number of scans for a generated storm.");
  tt->val_offset = (char *) &(pp.scan_interval) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = 0;
  tt->max_val.d = 1800;
  tt->single_val.d = 360;
  tt++;
  
  /* Parameter 'dbz_threshold' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("dbz_threshold");
  tt->descr = tdrpStrDup("Lower dBZ threshold.");
  tt->help = tdrpStrDup("This value is added to the dbzMax values (which are relative to the threshold) to produce the dbzMax.");
  tt->val_offset = (char *) &(pp.dbz_threshold) - (char *) &pp;
  tt->has_min = TRUE;
  tt->has_max = TRUE;
  tt->min_val.d = -30;
  tt->max_val.d = 80;
  tt->single_val.d = 35;
  tt++;
  
  /* Parameter 'mid_point_file_path' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("mid_point_file_path");
  tt->descr = tdrpStrDup("MDV file path for start point data.");
  tt->help = tdrpStrDup("The model uses this data for randomly selecting storm start points.");
  tt->val_offset = (char *) &(pp.mid_point_file_path) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("none");
  tt++;
  
  /* Parameter 'mid_point_field_num' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("mid_point_field_num");
  tt->descr = tdrpStrDup("Field number for mid_point data in MDV file.");
  tt->help = tdrpStrDup("See mid_point_file_path.");
  tt->val_offset = (char *) &(pp.mid_point_field_num) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'mid_point_sdev' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("mid_point_sdev");
  tt->descr = tdrpStrDup("Standard deviation for start point location.");
  tt->help = tdrpStrDup("First a mid_point is generated from the MDV cumulative density function for mid_point data. Then the position is varied randomly in x and y by assuming a normal spread with this sdev.");
  tt->val_offset = (char *) &(pp.mid_point_sdev) - (char *) &pp;
  tt->single_val.d = 10;
  tt++;
  
  /* Parameter 'duration_file_path' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("duration_file_path");
  tt->descr = tdrpStrDup("MDV file path for duration data.");
  tt->help = tdrpStrDup("The model uses this data for generating storm duration. A duration is generated from the Gamma distribution, and the result is scaled by the duration at the point over the mean duration for the entire grid.");
  tt->val_offset = (char *) &(pp.duration_file_path) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("none");
  tt++;
  
  /* Parameter 'duration_field_num' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("duration_field_num");
  tt->descr = tdrpStrDup("Field number for duration data in MDV file.");
  tt->help = tdrpStrDup("See duration_file_path.");
  tt->val_offset = (char *) &(pp.duration_field_num) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'ln_area_file_path' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("ln_area_file_path");
  tt->descr = tdrpStrDup("MDV file path for ln(area) data.");
  tt->help = tdrpStrDup("The model uses this data for generating storm area. ln(area) is generated from a gamma distribution conditioned on the Dm variable.The result is scaled by the ratio of ln(area) at the point over the mean for the entire grid.");
  tt->val_offset = (char *) &(pp.ln_area_file_path) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("none");
  tt++;
  
  /* Parameter 'ln_area_field_num' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("ln_area_field_num");
  tt->descr = tdrpStrDup("Field number for ln area data in MDV file.");
  tt->help = tdrpStrDup("See ln_area_file_path.");
  tt->val_offset = (char *) &(pp.ln_area_field_num) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'velocity_file_path' */
  /* ctype is 'char*' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRING_TYPE;
  tt->param_name = tdrpStrDup("velocity_file_path");
  tt->descr = tdrpStrDup("MDV file path for mean U and V data.");
  tt->help = tdrpStrDup("For a given point, selected from the mid_point array, the mean U and V data is determined. Then, the model U and V is selected using these mean values, and the area-wide standard deviations of the U and V fields.");
  tt->val_offset = (char *) &(pp.velocity_file_path) - (char *) &pp;
  tt->single_val.s = tdrpStrDup("none");
  tt++;
  
  /* Parameter 'u_mean_field_num' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("u_mean_field_num");
  tt->descr = tdrpStrDup("Field number for mean U data in MDV file.");
  tt->help = tdrpStrDup("See velocity_file_path.");
  tt->val_offset = (char *) &(pp.u_mean_field_num) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'v_mean_field_num' */
  /* ctype is 'long' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = LONG_TYPE;
  tt->param_name = tdrpStrDup("v_mean_field_num");
  tt->descr = tdrpStrDup("Field number for mean V data in MDV file.");
  tt->help = tdrpStrDup("See velocity_file_path.");
  tt->val_offset = (char *) &(pp.v_mean_field_num) - (char *) &pp;
  tt->single_val.l = 0;
  tt++;
  
  /* Parameter 'u_sdev' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("u_sdev");
  tt->descr = tdrpStrDup("Standard deviation for U data.");
  tt->help = tdrpStrDup("See velocity_file_path.");
  tt->val_offset = (char *) &(pp.u_sdev) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'v_sdev' */
  /* ctype is 'double' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = DOUBLE_TYPE;
  tt->param_name = tdrpStrDup("v_sdev");
  tt->descr = tdrpStrDup("Standard deviation for V data.");
  tt->help = tdrpStrDup("See velocity_file_path.");
  tt->val_offset = (char *) &(pp.v_sdev) - (char *) &pp;
  tt->single_val.d = 0;
  tt++;
  
  /* Parameter 'ln_ellipse_ratio_norm' */
  /* ctype is 'StormModel_ln_ellipse_ratio_norm' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("ln_ellipse_ratio_norm");
  tt->descr = tdrpStrDup("Normal distribution parameters for ln of ellipse ratio.");
  tt->help = tdrpStrDup("Ellipse ratio is the aspect ratio (major radius / minor radius) of the ellipse fitted to the storm shape. It is lognormally distributed");
  tt->val_offset = (char *) &(pp.ln_ellipse_ratio_norm) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("ln_ellipse_ratio_norm");
  tt->struct_def.nfields = 2;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("mean");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.ln_ellipse_ratio_norm.mean) - (char *) &(pp.ln_ellipse_ratio_norm);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("sdev");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.ln_ellipse_ratio_norm.sdev) - (char *) &(pp.ln_ellipse_ratio_norm);
  tt->n_struct_vals = 2;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
  tt++;
  
  /* Parameter 'ellipse_orientation_norm' */
  /* ctype is 'StormModel_ellipse_orientation_norm' */
  
  memset(tt, 0, sizeof(TDRPtable));
  tt->ptype = STRUCT_TYPE;
  tt->param_name = tdrpStrDup("ellipse_orientation_norm");
  tt->descr = tdrpStrDup("Normal distribution parameters for ellipse orientation.");
  tt->help = tdrpStrDup("Ellipse orientation is the orientation relative to TN  of the ellipse fitted to the storm shape. It is normally distributed");
  tt->val_offset = (char *) &(pp.ellipse_orientation_norm) - (char *) &pp;
  tt->struct_def.name = tdrpStrDup("ellipse_orientation_norm");
  tt->struct_def.nfields = 2;
  tt->struct_def.fields = (struct_field_t *)
      tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
    tt->struct_def.fields[0].ftype = tdrpStrDup("double");
    tt->struct_def.fields[0].fname = tdrpStrDup("mean");
    tt->struct_def.fields[0].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[0].rel_offset = 
      (char *) &(pp.ellipse_orientation_norm.mean) - (char *) &(pp.ellipse_orientation_norm);
    tt->struct_def.fields[1].ftype = tdrpStrDup("double");
    tt->struct_def.fields[1].fname = tdrpStrDup("sdev");
    tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
    tt->struct_def.fields[1].rel_offset = 
      (char *) &(pp.ellipse_orientation_norm.sdev) - (char *) &(pp.ellipse_orientation_norm);
  tt->n_struct_vals = 2;
  tt->struct_vals = (tdrpVal_t *)
      tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
    tt->struct_vals[0].d = 0;
    tt->struct_vals[1].d = 0;
  tt++;
  
  /* trailing entry has param_name set to NULL */
  
  tt->param_name = NULL;
  
  return (Table);

}
