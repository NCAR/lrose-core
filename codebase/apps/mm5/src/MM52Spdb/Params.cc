// *=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*
// ** Copyright UCAR (c) 1992 - 2017
// ** University Corporation for Atmospheric Research(UCAR)
// ** National Center for Atmospheric Research(NCAR)
// ** Boulder, Colorado, USA
// *=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*=*
////////////////////////////////////////////
// Params.cc
//
// TDRP C++ code file for class 'Params'.
//
// Code for program MM52Spdb
//
// This file has been automatically
// generated by TDRP, do not modify.
//
/////////////////////////////////////////////

/**
 *
 * @file Params.cc
 *
 * @class Params
 *
 * This class is automatically generated by the Table
 * Driven Runtime Parameters (TDRP) system
 *
 * @note Source is automatically generated from
 *       paramdef file at compile time, do not modify
 *       since modifications will be overwritten.
 *
 *
 * @author Automatically generated
 *
 */
using namespace std;

#include "Params.hh"
#include <cstring>

  ////////////////////////////////////////////
  // Default constructor
  //

  Params::Params()

  {

    // zero out table

    memset(_table, 0, sizeof(_table));

    // zero out members

    memset(&_start_, 0, &_end_ - &_start_);

    // class name

    _className = "Params";

    // initialize table

    _init();

    // set members

    tdrpTable2User(_table, &_start_);

    _exitDeferred = false;

  }

  ////////////////////////////////////////////
  // Copy constructor
  //

  Params::Params(const Params& source)

  {

    // sync the source object

    source.sync();

    // zero out table

    memset(_table, 0, sizeof(_table));

    // zero out members

    memset(&_start_, 0, &_end_ - &_start_);

    // class name

    _className = "Params";

    // copy table

    tdrpCopyTable((TDRPtable *) source._table, _table);

    // set members

    tdrpTable2User(_table, &_start_);

    _exitDeferred = false;

  }

  ////////////////////////////////////////////
  // Destructor
  //

  Params::~Params()

  {

    // free up

    freeAll();

  }

  ////////////////////////////////////////////
  // Assignment
  //

  void Params::operator=(const Params& other)

  {

    // sync the other object

    other.sync();

    // free up any existing memory

    freeAll();

    // zero out table

    memset(_table, 0, sizeof(_table));

    // zero out members

    memset(&_start_, 0, &_end_ - &_start_);

    // copy table

    tdrpCopyTable((TDRPtable *) other._table, _table);

    // set members

    tdrpTable2User(_table, &_start_);

    _exitDeferred = other._exitDeferred;

  }

  ////////////////////////////////////////////
  // loadFromArgs()
  //
  // Loads up TDRP using the command line args.
  //
  // Check usage() for command line actions associated with
  // this function.
  //
  //   argc, argv: command line args
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   char **params_path_p:
  //     If this is non-NULL, it is set to point to the path
  //     of the params file used.
  //
  //   bool defer_exit: normally, if the command args contain a 
  //      print or check request, this function will call exit().
  //      If defer_exit is set, such an exit is deferred and the
  //      private member _exitDeferred is set.
  //      Use exidDeferred() to test this flag.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadFromArgs(int argc, char **argv,
                           char **override_list,
                           char **params_path_p,
                           bool defer_exit)
  {
    int exit_deferred;
    if (_tdrpLoadFromArgs(argc, argv,
                          _table, &_start_,
                          override_list, params_path_p,
                          _className,
                          defer_exit, &exit_deferred)) {
      return (-1);
    } else {
      if (exit_deferred) {
        _exitDeferred = true;
      }
      return (0);
    }
  }

  ////////////////////////////////////////////
  // loadApplyArgs()
  //
  // Loads up TDRP using the params path passed in, and applies
  // the command line args for printing and checking.
  //
  // Check usage() for command line actions associated with
  // this function.
  //
  //   const char *param_file_path: the parameter file to be read in
  //
  //   argc, argv: command line args
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   bool defer_exit: normally, if the command args contain a 
  //      print or check request, this function will call exit().
  //      If defer_exit is set, such an exit is deferred and the
  //      private member _exitDeferred is set.
  //      Use exidDeferred() to test this flag.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadApplyArgs(const char *params_path,
                            int argc, char **argv,
                            char **override_list,
                            bool defer_exit)
  {
    int exit_deferred;
    if (tdrpLoadApplyArgs(params_path, argc, argv,
                          _table, &_start_,
                          override_list,
                          _className,
                          defer_exit, &exit_deferred)) {
      return (-1);
    } else {
      if (exit_deferred) {
        _exitDeferred = true;
      }
      return (0);
    }
  }

  ////////////////////////////////////////////
  // isArgValid()
  // 
  // Check if a command line arg is a valid TDRP arg.
  //

  bool Params::isArgValid(const char *arg)
  {
    return (tdrpIsArgValid(arg));
  }

  ////////////////////////////////////////////
  // load()
  //
  // Loads up TDRP for a given class.
  //
  // This version of load gives the programmer the option to load
  // up more than one class for a single application. It is a
  // lower-level routine than loadFromArgs, and hence more
  // flexible, but the programmer must do more work.
  //
  //   const char *param_file_path: the parameter file to be read in.
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   expand_env: flag to control environment variable
  //               expansion during tokenization.
  //               If TRUE, environment expansion is set on.
  //               If FALSE, environment expansion is set off.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::load(const char *param_file_path,
                   char **override_list,
                   int expand_env, int debug)
  {
    if (tdrpLoad(param_file_path,
                 _table, &_start_,
                 override_list,
                 expand_env, debug)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // loadFromBuf()
  //
  // Loads up TDRP for a given class.
  //
  // This version of load gives the programmer the option to
  // load up more than one module for a single application,
  // using buffers which have been read from a specified source.
  //
  //   const char *param_source_str: a string which describes the
  //     source of the parameter information. It is used for
  //     error reporting only.
  //
  //   char **override_list: A null-terminated list of overrides
  //     to the parameter file.
  //     An override string has exactly the format of an entry
  //     in the parameter file itself.
  //
  //   const char *inbuf: the input buffer
  //
  //   int inlen: length of the input buffer
  //
  //   int start_line_num: the line number in the source which
  //     corresponds to the start of the buffer.
  //
  //   expand_env: flag to control environment variable
  //               expansion during tokenization.
  //               If TRUE, environment expansion is set on.
  //               If FALSE, environment expansion is set off.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadFromBuf(const char *param_source_str,
                          char **override_list,
                          const char *inbuf, int inlen,
                          int start_line_num,
                          int expand_env, int debug)
  {
    if (tdrpLoadFromBuf(param_source_str,
                        _table, &_start_,
                        override_list,
                        inbuf, inlen, start_line_num,
                        expand_env, debug)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // loadDefaults()
  //
  // Loads up default params for a given class.
  //
  // See load() for more detailed info.
  //
  //  Returns 0 on success, -1 on failure.
  //

  int Params::loadDefaults(int expand_env)
  {
    if (tdrpLoad(NULL,
                 _table, &_start_,
                 NULL, expand_env, FALSE)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // sync()
  //
  // Syncs the user struct data back into the parameter table,
  // in preparation for printing.
  //
  // This function alters the table in a consistent manner.
  // Therefore it can be regarded as const.
  //

  void Params::sync(void) const
  {
    tdrpUser2Table(_table, (char *) &_start_);
  }

  ////////////////////////////////////////////
  // print()
  // 
  // Print params file
  //
  // The modes supported are:
  //
  //   PRINT_SHORT:   main comments only, no help or descriptions
  //                  structs and arrays on a single line
  //   PRINT_NORM:    short + descriptions and help
  //   PRINT_LONG:    norm  + arrays and structs expanded
  //   PRINT_VERBOSE: long  + private params included
  //

  void Params::print(FILE *out, tdrp_print_mode_t mode)
  {
    tdrpPrint(out, _table, _className, mode);
  }

  ////////////////////////////////////////////
  // checkAllSet()
  //
  // Return TRUE if all set, FALSE if not.
  //
  // If out is non-NULL, prints out warning messages for those
  // parameters which are not set.
  //

  int Params::checkAllSet(FILE *out)
  {
    return (tdrpCheckAllSet(out, _table, &_start_));
  }

  //////////////////////////////////////////////////////////////
  // checkIsSet()
  //
  // Return TRUE if parameter is set, FALSE if not.
  //
  //

  int Params::checkIsSet(const char *paramName)
  {
    return (tdrpCheckIsSet(paramName, _table, &_start_));
  }

  ////////////////////////////////////////////
  // freeAll()
  //
  // Frees up all TDRP dynamic memory.
  //

  void Params::freeAll(void)
  {
    tdrpFreeAll(_table, &_start_);
  }

  ////////////////////////////////////////////
  // usage()
  //
  // Prints out usage message for TDRP args as passed
  // in to loadFromArgs().
  //

  void Params::usage(ostream &out)
  {
    out << "TDRP args: [options as below]\n"
        << "   [ -params/--params path ] specify params file path\n"
        << "   [ -check_params/--check_params] check which params are not set\n"
        << "   [ -print_params/--print_params [mode]] print parameters\n"
        << "     using following modes, default mode is 'norm'\n"
        << "       short:   main comments only, no help or descr\n"
        << "                structs and arrays on a single line\n"
        << "       norm:    short + descriptions and help\n"
        << "       long:    norm  + arrays and structs expanded\n"
        << "       verbose: long  + private params included\n"
        << "       short_expand:   short with env vars expanded\n"
        << "       norm_expand:    norm with env vars expanded\n"
        << "       long_expand:    long with env vars expanded\n"
        << "       verbose_expand: verbose with env vars expanded\n"
        << "   [ -tdrp_debug] debugging prints for tdrp\n"
        << "   [ -tdrp_usage] print this usage\n";
  }

  ////////////////////////////////////////////
  // arrayRealloc()
  //
  // Realloc 1D array.
  //
  // If size is increased, the values from the last array 
  // entry is copied into the new space.
  //
  // Returns 0 on success, -1 on error.
  //

  int Params::arrayRealloc(const char *param_name, int new_array_n)
  {
    if (tdrpArrayRealloc(_table, &_start_,
                         param_name, new_array_n)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // array2DRealloc()
  //
  // Realloc 2D array.
  //
  // If size is increased, the values from the last array 
  // entry is copied into the new space.
  //
  // Returns 0 on success, -1 on error.
  //

  int Params::array2DRealloc(const char *param_name,
                             int new_array_n1,
                             int new_array_n2)
  {
    if (tdrpArray2DRealloc(_table, &_start_, param_name,
                           new_array_n1, new_array_n2)) {
      return (-1);
    } else {
      return (0);
    }
  }

  ////////////////////////////////////////////
  // _init()
  //
  // Class table initialization function.
  //
  //

  void Params::_init()

  {

    TDRPtable *tt = _table;

    // Parameter 'Comment 0'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 0");
    tt->comment_hdr = tdrpStrDup("MM52Spdb");
    tt->comment_text = tdrpStrDup("MM52Spdb interpolates MM5 data to specified points and saves out the data to an SPDB database as a ComboPt buffer.");
    tt++;
    
    // Parameter 'Comment 1'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 1");
    tt->comment_hdr = tdrpStrDup("DEBUGGING AND PROCESS CONTROL");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'debug'
    // ctype is '_debug_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("debug");
    tt->descr = tdrpStrDup("Debug option");
    tt->help = tdrpStrDup("If set, debug messages will be printed with the appropriate level of detail.");
    tt->val_offset = (char *) &debug - &_start_;
    tt->enum_def.name = tdrpStrDup("debug_t");
    tt->enum_def.nfields = 4;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("DEBUG_OFF");
      tt->enum_def.fields[0].val = DEBUG_OFF;
      tt->enum_def.fields[1].name = tdrpStrDup("DEBUG_WARNINGS");
      tt->enum_def.fields[1].val = DEBUG_WARNINGS;
      tt->enum_def.fields[2].name = tdrpStrDup("DEBUG_NORM");
      tt->enum_def.fields[2].val = DEBUG_NORM;
      tt->enum_def.fields[3].name = tdrpStrDup("DEBUG_VERBOSE");
      tt->enum_def.fields[3].val = DEBUG_VERBOSE;
    tt->single_val.e = DEBUG_OFF;
    tt++;
    
    // Parameter 'instance'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("instance");
    tt->descr = tdrpStrDup("Process instance");
    tt->help = tdrpStrDup("Used for registration with procmap.");
    tt->val_offset = (char *) &instance - &_start_;
    tt->single_val.s = tdrpStrDup("Test");
    tt++;
    
    // Parameter 'Comment 2'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 2");
    tt->comment_hdr = tdrpStrDup("PROGRAM MODES OF OPERATION.");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'mode'
    // ctype is '_mode_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("mode");
    tt->descr = tdrpStrDup("Operation mode");
    tt->help = tdrpStrDup("Program may be run in two modes, archive and realtime. In realtime mode, the analysis is performed for each volume scan as it becomes available. In archive mode, the file list is obtained from the command line.");
    tt->val_offset = (char *) &mode - &_start_;
    tt->enum_def.name = tdrpStrDup("mode_t");
    tt->enum_def.nfields = 2;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("ARCHIVE");
      tt->enum_def.fields[0].val = ARCHIVE;
      tt->enum_def.fields[1].name = tdrpStrDup("REALTIME");
      tt->enum_def.fields[1].val = REALTIME;
    tt->single_val.e = ARCHIVE;
    tt++;
    
    // Parameter 'Comment 3'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 3");
    tt->comment_hdr = tdrpStrDup("INPUT DATA.");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'realtime_input_dir'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("realtime_input_dir");
    tt->descr = tdrpStrDup("Directory for input MM5 data - REALTIME mode.");
    tt->help = tdrpStrDup("The latest_data_info file in this directory is updated each time the model adds a forecast time step to the output file.");
    tt->val_offset = (char *) &realtime_input_dir - &_start_;
    tt->single_val.s = tdrpStrDup(".");
    tt++;
    
    // Parameter 'max_realtime_valid_age'
    // ctype is 'int'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = INT_TYPE;
    tt->param_name = tdrpStrDup("max_realtime_valid_age");
    tt->descr = tdrpStrDup("Max age of input data - REALTIME mode.");
    tt->help = tdrpStrDup("For realtime only - max age of input data for it to be valid. If the latest_data_info file is older than this, the program waits for new data.");
    tt->val_offset = (char *) &max_realtime_valid_age - &_start_;
    tt->has_min = TRUE;
    tt->min_val.i = 0;
    tt->single_val.i = 300;
    tt++;
    
    // Parameter 'Comment 4'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = COMMENT_TYPE;
    tt->param_name = tdrpStrDup("Comment 4");
    tt->comment_hdr = tdrpStrDup("OUTPUT DATA.");
    tt->comment_text = tdrpStrDup("");
    tt++;
    
    // Parameter 'output_url'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("output_url");
    tt->descr = tdrpStrDup("Output URL.");
    tt->help = tdrpStrDup("The interpolated point data is output to this URL in SPDB format.");
    tt->val_offset = (char *) &output_url - &_start_;
    tt->single_val.s = tdrpStrDup(".");
    tt++;
    
    // Parameter 'output_fields_3d'
    // ctype is '_output_field_3d_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("output_fields_3d");
    tt->descr = tdrpStrDup("3D Output fields.");
    tt->help = tdrpStrDup("In the output there will be one value for each of these fields for each height available at each point.");
    tt->array_offset = (char *) &_output_fields_3d - &_start_;
    tt->array_n_offset = (char *) &output_fields_3d_n - &_start_;
    tt->is_array = TRUE;
    tt->array_len_fixed = FALSE;
    tt->array_elem_size = sizeof(output_field_3d_t);
    tt->array_n = 10;
    tt->enum_def.name = tdrpStrDup("output_field_3d_t");
    tt->enum_def.nfields = 10;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("U_3D");
      tt->enum_def.fields[0].val = U_3D;
      tt->enum_def.fields[1].name = tdrpStrDup("V_3D");
      tt->enum_def.fields[1].val = V_3D;
      tt->enum_def.fields[2].name = tdrpStrDup("W_3D");
      tt->enum_def.fields[2].val = W_3D;
      tt->enum_def.fields[3].name = tdrpStrDup("TEMP_3D");
      tt->enum_def.fields[3].val = TEMP_3D;
      tt->enum_def.fields[4].name = tdrpStrDup("HUMIDITY_3D");
      tt->enum_def.fields[4].val = HUMIDITY_3D;
      tt->enum_def.fields[5].name = tdrpStrDup("CLW_3D");
      tt->enum_def.fields[5].val = CLW_3D;
      tt->enum_def.fields[6].name = tdrpStrDup("RNW_3D");
      tt->enum_def.fields[6].val = RNW_3D;
      tt->enum_def.fields[7].name = tdrpStrDup("ICE_3D");
      tt->enum_def.fields[7].val = ICE_3D;
      tt->enum_def.fields[8].name = tdrpStrDup("SNOW_3D");
      tt->enum_def.fields[8].val = SNOW_3D;
      tt->enum_def.fields[9].name = tdrpStrDup("RAD_TEND_3D");
      tt->enum_def.fields[9].val = RAD_TEND_3D;
    tt->array_vals = (tdrpVal_t *)
        tdrpMalloc(tt->array_n * sizeof(tdrpVal_t));
      tt->array_vals[0].e = U_3D;
      tt->array_vals[1].e = V_3D;
      tt->array_vals[2].e = W_3D;
      tt->array_vals[3].e = TEMP_3D;
      tt->array_vals[4].e = HUMIDITY_3D;
      tt->array_vals[5].e = CLW_3D;
      tt->array_vals[6].e = RNW_3D;
      tt->array_vals[7].e = ICE_3D;
      tt->array_vals[8].e = SNOW_3D;
      tt->array_vals[9].e = RAD_TEND_3D;
    tt++;
    
    // Parameter 'output_fields_2d'
    // ctype is '_output_field_2d_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = ENUM_TYPE;
    tt->param_name = tdrpStrDup("output_fields_2d");
    tt->descr = tdrpStrDup("2D Output fields.");
    tt->help = tdrpStrDup("In the output there will be one value for each of these fields at each  point.");
    tt->array_offset = (char *) &_output_fields_2d - &_start_;
    tt->array_n_offset = (char *) &output_fields_2d_n - &_start_;
    tt->is_array = TRUE;
    tt->array_len_fixed = FALSE;
    tt->array_elem_size = sizeof(output_field_2d_t);
    tt->array_n = 22;
    tt->enum_def.name = tdrpStrDup("output_field_2d_t");
    tt->enum_def.nfields = 22;
    tt->enum_def.fields = (enum_field_t *)
        tdrpMalloc(tt->enum_def.nfields * sizeof(enum_field_t));
      tt->enum_def.fields[0].name = tdrpStrDup("GROUND_T_2D");
      tt->enum_def.fields[0].val = GROUND_T_2D;
      tt->enum_def.fields[1].name = tdrpStrDup("RAIN_CON_2D");
      tt->enum_def.fields[1].val = RAIN_CON_2D;
      tt->enum_def.fields[2].name = tdrpStrDup("RAIN_NON_2D");
      tt->enum_def.fields[2].val = RAIN_NON_2D;
      tt->enum_def.fields[3].name = tdrpStrDup("TERRAIN_2D");
      tt->enum_def.fields[3].val = TERRAIN_2D;
      tt->enum_def.fields[4].name = tdrpStrDup("CORIOLIS_2D");
      tt->enum_def.fields[4].val = CORIOLIS_2D;
      tt->enum_def.fields[5].name = tdrpStrDup("RES_TEMP_2D");
      tt->enum_def.fields[5].val = RES_TEMP_2D;
      tt->enum_def.fields[6].name = tdrpStrDup("LAND_USE_2D");
      tt->enum_def.fields[6].val = LAND_USE_2D;
      tt->enum_def.fields[7].name = tdrpStrDup("SNOWCOVR_2D");
      tt->enum_def.fields[7].val = SNOWCOVR_2D;
      tt->enum_def.fields[8].name = tdrpStrDup("TSEASFC_2D");
      tt->enum_def.fields[8].val = TSEASFC_2D;
      tt->enum_def.fields[9].name = tdrpStrDup("PBL_HGT_2D");
      tt->enum_def.fields[9].val = PBL_HGT_2D;
      tt->enum_def.fields[10].name = tdrpStrDup("REGIME_2D");
      tt->enum_def.fields[10].val = REGIME_2D;
      tt->enum_def.fields[11].name = tdrpStrDup("SHFLUX_2D");
      tt->enum_def.fields[11].val = SHFLUX_2D;
      tt->enum_def.fields[12].name = tdrpStrDup("LHFLUX_2D");
      tt->enum_def.fields[12].val = LHFLUX_2D;
      tt->enum_def.fields[13].name = tdrpStrDup("UST_2D");
      tt->enum_def.fields[13].val = UST_2D;
      tt->enum_def.fields[14].name = tdrpStrDup("SWDOWN_2D");
      tt->enum_def.fields[14].val = SWDOWN_2D;
      tt->enum_def.fields[15].name = tdrpStrDup("LWDOWN_2D");
      tt->enum_def.fields[15].val = LWDOWN_2D;
      tt->enum_def.fields[16].name = tdrpStrDup("SOIL_T_1_2D");
      tt->enum_def.fields[16].val = SOIL_T_1_2D;
      tt->enum_def.fields[17].name = tdrpStrDup("SOIL_T_2_2D");
      tt->enum_def.fields[17].val = SOIL_T_2_2D;
      tt->enum_def.fields[18].name = tdrpStrDup("SOIL_T_3_2D");
      tt->enum_def.fields[18].val = SOIL_T_3_2D;
      tt->enum_def.fields[19].name = tdrpStrDup("SOIL_T_4_2D");
      tt->enum_def.fields[19].val = SOIL_T_4_2D;
      tt->enum_def.fields[20].name = tdrpStrDup("SOIL_T_5_2D");
      tt->enum_def.fields[20].val = SOIL_T_5_2D;
      tt->enum_def.fields[21].name = tdrpStrDup("SOIL_T_6_2D");
      tt->enum_def.fields[21].val = SOIL_T_6_2D;
    tt->array_vals = (tdrpVal_t *)
        tdrpMalloc(tt->array_n * sizeof(tdrpVal_t));
      tt->array_vals[0].e = GROUND_T_2D;
      tt->array_vals[1].e = RAIN_CON_2D;
      tt->array_vals[2].e = RAIN_NON_2D;
      tt->array_vals[3].e = TERRAIN_2D;
      tt->array_vals[4].e = CORIOLIS_2D;
      tt->array_vals[5].e = RES_TEMP_2D;
      tt->array_vals[6].e = LAND_USE_2D;
      tt->array_vals[7].e = SNOWCOVR_2D;
      tt->array_vals[8].e = TSEASFC_2D;
      tt->array_vals[9].e = PBL_HGT_2D;
      tt->array_vals[10].e = REGIME_2D;
      tt->array_vals[11].e = SHFLUX_2D;
      tt->array_vals[12].e = LHFLUX_2D;
      tt->array_vals[13].e = UST_2D;
      tt->array_vals[14].e = SWDOWN_2D;
      tt->array_vals[15].e = LWDOWN_2D;
      tt->array_vals[16].e = SOIL_T_1_2D;
      tt->array_vals[17].e = SOIL_T_2_2D;
      tt->array_vals[18].e = SOIL_T_3_2D;
      tt->array_vals[19].e = SOIL_T_4_2D;
      tt->array_vals[20].e = SOIL_T_5_2D;
      tt->array_vals[21].e = SOIL_T_6_2D;
    tt++;
    
    // Parameter 'output_points'
    // ctype is '_output_point_t'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRUCT_TYPE;
    tt->param_name = tdrpStrDup("output_points");
    tt->descr = tdrpStrDup("Name and location of output points.");
    tt->help = tdrpStrDup("The model data will be interpolated to each of these locations.");
    tt->array_offset = (char *) &_output_points - &_start_;
    tt->array_n_offset = (char *) &output_points_n - &_start_;
    tt->is_array = TRUE;
    tt->array_len_fixed = FALSE;
    tt->array_elem_size = sizeof(output_point_t);
    tt->array_n = 29;
    tt->struct_def.name = tdrpStrDup("output_point_t");
    tt->struct_def.nfields = 4;
    tt->struct_def.fields = (struct_field_t *)
        tdrpMalloc(tt->struct_def.nfields * sizeof(struct_field_t));
      tt->struct_def.fields[0].ftype = tdrpStrDup("string");
      tt->struct_def.fields[0].fname = tdrpStrDup("name");
      tt->struct_def.fields[0].ptype = STRING_TYPE;
      tt->struct_def.fields[0].rel_offset = 
        (char *) &_output_points->name - (char *) _output_points;
      tt->struct_def.fields[1].ftype = tdrpStrDup("double");
      tt->struct_def.fields[1].fname = tdrpStrDup("lat");
      tt->struct_def.fields[1].ptype = DOUBLE_TYPE;
      tt->struct_def.fields[1].rel_offset = 
        (char *) &_output_points->lat - (char *) _output_points;
      tt->struct_def.fields[2].ftype = tdrpStrDup("double");
      tt->struct_def.fields[2].fname = tdrpStrDup("lon");
      tt->struct_def.fields[2].ptype = DOUBLE_TYPE;
      tt->struct_def.fields[2].rel_offset = 
        (char *) &_output_points->lon - (char *) _output_points;
      tt->struct_def.fields[3].ftype = tdrpStrDup("double");
      tt->struct_def.fields[3].fname = tdrpStrDup("alt");
      tt->struct_def.fields[3].ptype = DOUBLE_TYPE;
      tt->struct_def.fields[3].rel_offset = 
        (char *) &_output_points->alt - (char *) _output_points;
    tt->n_struct_vals = 116;
    tt->struct_vals = (tdrpVal_t *)
        tdrpMalloc(tt->n_struct_vals * sizeof(tdrpVal_t));
      tt->struct_vals[0].s = tdrpStrDup("RCTP");
      tt->struct_vals[1].d = 25.08;
      tt->struct_vals[2].d = 121.22;
      tt->struct_vals[3].d = 33;
      tt->struct_vals[4].s = tdrpStrDup("RCFG");
      tt->struct_vals[5].d = 26.17;
      tt->struct_vals[6].d = 119.93;
      tt->struct_vals[7].d = 91;
      tt->struct_vals[8].s = tdrpStrDup("RCSS");
      tt->struct_vals[9].d = 25.07;
      tt->struct_vals[10].d = 121.55;
      tt->struct_vals[11].d = 6;
      tt->struct_vals[12].s = tdrpStrDup("RCGM");
      tt->struct_vals[13].d = 25.07;
      tt->struct_vals[14].d = 121.23;
      tt->struct_vals[15].d = 45;
      tt->struct_vals[16].s = tdrpStrDup("RCNO");
      tt->struct_vals[17].d = 23.27;
      tt->struct_vals[18].d = 119.67;
      tt->struct_vals[19].d = 45;
      tt->struct_vals[20].s = tdrpStrDup("RCQC");
      tt->struct_vals[21].d = 23.58;
      tt->struct_vals[22].d = 119.62;
      tt->struct_vals[23].d = 31;
      tt->struct_vals[24].s = tdrpStrDup("RCBS");
      tt->struct_vals[25].d = 24.43;
      tt->struct_vals[26].d = 118.37;
      tt->struct_vals[27].d = 9;
      tt->struct_vals[28].s = tdrpStrDup("RCUK");
      tt->struct_vals[29].d = 24.93;
      tt->struct_vals[30].d = 121.3;
      tt->struct_vals[31].d = 141;
      tt->struct_vals[32].s = tdrpStrDup("RCKH");
      tt->struct_vals[33].d = 22.58;
      tt->struct_vals[34].d = 120.35;
      tt->struct_vals[35].d = 9;
      tt->struct_vals[36].s = tdrpStrDup("RCFN");
      tt->struct_vals[37].d = 22.77;
      tt->struct_vals[38].d = 121.08;
      tt->struct_vals[39].d = 42;
      tt->struct_vals[40].s = tdrpStrDup("RCNN");
      tt->struct_vals[41].d = 22.95;
      tt->struct_vals[42].d = 120.2;
      tt->struct_vals[43].d = 19;
      tt->struct_vals[44].s = tdrpStrDup("RCAY");
      tt->struct_vals[45].d = 22.78;
      tt->struct_vals[46].d = 120.27;
      tt->struct_vals[47].d = 10;
      tt->struct_vals[48].s = tdrpStrDup("RCKU");
      tt->struct_vals[49].d = 23.47;
      tt->struct_vals[50].d = 120.38;
      tt->struct_vals[51].d = 25;
      tt->struct_vals[52].s = tdrpStrDup("RCMJ");
      tt->struct_vals[53].d = 22.47;
      tt->struct_vals[54].d = 120.43;
      tt->struct_vals[55].d = 8;
      tt->struct_vals[56].s = tdrpStrDup("RCDC");
      tt->struct_vals[57].d = 22.68;
      tt->struct_vals[58].d = 120.47;
      tt->struct_vals[59].d = 24;
      tt->struct_vals[60].s = tdrpStrDup("RCLG");
      tt->struct_vals[61].d = 24.18;
      tt->struct_vals[62].d = 120.65;
      tt->struct_vals[63].d = 112;
      tt->struct_vals[64].s = tdrpStrDup("RCKW");
      tt->struct_vals[65].d = 21.93;
      tt->struct_vals[66].d = 120.83;
      tt->struct_vals[67].d = 13;
      tt->struct_vals[68].s = tdrpStrDup("RCPO");
      tt->struct_vals[69].d = 24.82;
      tt->struct_vals[70].d = 120.93;
      tt->struct_vals[71].d = 8;
      tt->struct_vals[72].s = tdrpStrDup("RCSQ");
      tt->struct_vals[73].d = 22.7;
      tt->struct_vals[74].d = 120.48;
      tt->struct_vals[75].d = 29;
      tt->struct_vals[76].s = tdrpStrDup("RCQS");
      tt->struct_vals[77].d = 22.8;
      tt->struct_vals[78].d = 121.18;
      tt->struct_vals[79].d = 37;
      tt->struct_vals[80].s = tdrpStrDup("RCLY");
      tt->struct_vals[81].d = 22.03;
      tt->struct_vals[82].d = 121.55;
      tt->struct_vals[83].d = 325;
      tt->struct_vals[84].s = tdrpStrDup("RCYU");
      tt->struct_vals[85].d = 24.03;
      tt->struct_vals[86].d = 121.62;
      tt->struct_vals[87].d = 16;
      tt->struct_vals[88].s = tdrpStrDup("RCMS");
      tt->struct_vals[89].d = 24.75;
      tt->struct_vals[90].d = 121.77;
      tt->struct_vals[91].d = 9;
      tt->struct_vals[92].s = tdrpStrDup("RCDI");
      tt->struct_vals[93].d = 24.87;
      tt->struct_vals[94].d = 121.23;
      tt->struct_vals[95].d = 241;
      tt->struct_vals[96].s = tdrpStrDup("RCMQ");
      tt->struct_vals[97].d = 24.27;
      tt->struct_vals[98].d = 120.62;
      tt->struct_vals[99].d = 5;
      tt->struct_vals[100].s = tdrpStrDup("RCFS");
      tt->struct_vals[101].d = 22.42;
      tt->struct_vals[102].d = 120.55;
      tt->struct_vals[103].d = 20;
      tt->struct_vals[104].s = tdrpStrDup("RCGI");
      tt->struct_vals[105].d = 22.67;
      tt->struct_vals[106].d = 121.46;
      tt->struct_vals[107].d = 25;
      tt->struct_vals[108].s = tdrpStrDup("RCMT");
      tt->struct_vals[109].d = 26.23;
      tt->struct_vals[110].d = 119.99;
      tt->struct_vals[111].d = 50;
      tt->struct_vals[112].s = tdrpStrDup("RCLM");
      tt->struct_vals[113].d = 20.67;
      tt->struct_vals[114].d = 116.72;
      tt->struct_vals[115].d = 6;
    tt++;
    
    // Parameter 'data_set_info'
    // ctype is 'char*'
    
    memset(tt, 0, sizeof(TDRPtable));
    tt->ptype = STRING_TYPE;
    tt->param_name = tdrpStrDup("data_set_info");
    tt->descr = tdrpStrDup("Data set info.");
    tt->help = tdrpStrDup("This text is placed in the product info part of the output data buffer for documentation purposes.");
    tt->val_offset = (char *) &data_set_info - &_start_;
    tt->single_val.s = tdrpStrDup("MM5 output data interpolated to this point.");
    tt++;
    
    // trailing entry has param_name set to NULL
    
    tt->param_name = NULL;
    
    return;
  
  }
